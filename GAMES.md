游戏设计：选什么、怎么做、悬疑感从哪来

本文档回答四件事：还需要哪些游戏、赛马这类交互游戏怎么落地、
悬疑感与画面特效如何实现、爬塔与房间搭建现状。

结论先说：**你提的超能力赛马是个好设计，但现有模板接口装不下它**，
需要先做一次契约层扩展。原因见第三节。

---

## 一、先看清现状（实测结论）

### 现有五个游戏

| 游戏 | 位置 | 状态 | 联机 |
|---|---|---|---|
| 月影狼人杀 | `src/engine/werewolfEngine.ts` 1041 行 | ✅ 可玩 | ❌ 纯前端 |
| 猜平均数 | `api/games/sdk/numberGuess.ts` | ✅ 可玩 | ✅ |
| 红眼病投票 | `api/games/sdk/pollDuel.ts` | ✅ 可玩 | ✅ |
| 丹丘牌楼 | `src/engine/poker/` 823 行 | ✅ 可玩 | 单人 |
| 碎境爬塔 | `src/engine/spire/` 1142 行 | ✅ 可玩 | 单人 |

### 爬塔有两套，尚未打通 ⚠️

这是个容易踩的坑，我实测确认过：

- **契约层的塔**（`contracts/spire.ts`）：48 层谜题链，四位阶各 12 层
  对应十二生肖，门禁由残章/判例/自创游戏守着。**设计极好，但 48 层的
  `FloorDef` 数据文件不存在**——只有契约和测试引用它。
- **实际在跑的塔**（`src/data/spire/` + `src/engine/spire/`）：
  类杀戮尖塔的单人战斗，15 行分支地图，有敌人/遗物/事件/商店。
  **这套完整可玩，但不读契约层的门禁**。

也就是说：`contracts/spire.ts` 里「卡在 A 层掉落、在 B 层才有用」
那套精妙的谜题链设计，目前一行数据都没有。

### 房间搭建：只有 UI 外壳

`CreateRoomModal.tsx` 有房名、门票、影从填充、开放观战、允许委托等开关，
但**观战开关是纯 UI state**，大厅的「观战」按钮点了会跳到一个新对局，
不是真观战。

---

## 二、还需要哪些游戏

选游戏的标准不是「好玩」，而是**「AI 之间的高水平智斗能不能被观众看懂」**。
按这个标准，我推荐五个，按优先级排：

### P0 · 超能力赛马（你的原创，最有观赏价值）

理由在第三节详述。一句话：**它是唯一能让观众在同一画面里
同时看到「多方博弈」和「因果链」的玩法**。

### P0 · 海盗分金（♦ 金壤）

规则书我已写好（`RB_PIRATE`）。它的价值是**唯一能体现联盟与背叛的博弈**，
且人的非理性正是看点——所以归 `mixed-required` 档。

观赏点：提案者要算清「给谁多少才能买到票」，而被提案者要判断
「现在接受还是赌下一轮能拿更多」。AI 的算计肉眼可见。

### P1 · 达芬奇密码（你提的，很适合）

**这个游戏出奇地适合 AI 对抗**，理由是它的信息结构极干净：

- 每人手里若干张数字牌，**自己看不见自己的，别人能看见**
- 轮流猜某人某张牌的数字，猜中则该牌翻开
- 信息完全公开且离散，没有自然语言歧义

为什么适合 AI：**它是纯贝叶斯推理**。AI 能算出「在已知翻开牌的条件下，
对手第 3 张是 7 的概率」，而这个概率可以可视化成一张热力图。
观众能直接看到 AI 脑子里的概率分布在每一轮如何收缩——
这是极好的观赏效果，比看人皱眉思考强得多。

改造建议：加一个「概率视图」开关，观战时能看到每个 AI 对
每张未知牌的概率估计。**这是本作独有的演出**，桌游原版做不到。

档位：`human-friendly`。人也能玩，但 AI 会明显更强，这本身就是看点。

### P1 · 千轮猜数（`agent-only`）

现有猜平均数的极端版：1000 轮，只收 Agent。

价值是**做人类做不到的事**：观众能看到一条 level-k 收敛曲线，
从初始的随机散布逐渐收缩到纳什均衡附近。这是纯 AI 对抗独有的美感。

实现成本极低——`numberGuess` 模板改个轮数上限即可，
但要把 `submitWindowSec` 下限放开到 1 秒，否则 1000 轮跑不完。

### P2 · 囚徒困境循环赛（`agent-only`）

200 轮迭代，每轮双方同时选合作/背叛。价值是**策略演化可视化**：
观众看到「一报还一报」如何战胜「永远背叛」，或者反之。

规则最简，观众三十秒看懂，适合暖场。

### 不推荐的：阿瓦隆

我改变了之前的看法。它和狼人杀太像，机制上没有提供新的观赏维度，
而开发成本不低（有任务队列、多轮投票、刺杀阶段）。
72 小时内做它不如把狼人杀联机化做扎实。

---

## 三、超能力赛马：设计与落地

### 为什么它值得做

你的设计里有三个层次，这是它比其他游戏好的地方：

1. **选动物** = 选异能流派（先验信息，公开可见）
2. **打技能卡** = 每轮的战术决策（秘密提交，同时揭晓）
3. **格子事件** = 环境变量（引入不确定性，制造戏剧性）

三层叠起来产生**因果链**：飞行的被打下来、领先的踩到陷阱、
落后的借沼泽反超。观众不需要理解博弈论就能看懂"发生了什么"，
但 AI 的决策深度又足够——这个组合很难得。

### 关键约束：现有模板接口装不下它 ⚠️

我查了 `api/games/sdk/templates.ts` 与 `contracts/room.ts`，现状是：

```ts
// 一轮内的一条提交，value 只能是单个数字
interface RoundEntry { seat: number; value: number; order: number }

// 动作只有三种
type GuessAction =
  | { type: "start" }
  | { type: "submit"; value: number }
  | { type: "choose"; choice: number };
```

赛马需要的是 `{ cardId, targetSeat }`（打哪张卡、对谁用），
还需要跨轮持久状态（每匹马的位置、状态层、冷却）。
**这两样现有接口都没有。**

### 需要的契约层扩展

这是做赛马前必须先做的事，且它对未来所有复杂游戏都有用：

```ts
// contracts/room.ts 扩展
export type GameAction =
  | { type: "start" }
  | { type: "submit"; value: number }
  | { type: "choose"; choice: number }
  // 新增：打牌（含目标）
  | { type: "play"; cardId: string; targetSeat?: number };

// api/games/sdk/templates.ts 扩展
export interface RoundEntry {
  seat: number;
  value: number;
  order: number;
  /** 新增：结构化载荷，模板自定义 */
  payload?: unknown;
}

export interface TemplateModule {
  // ... 现有方法
  /**
   * 新增（可选）：跨轮持久状态。
   * 返回初始状态；resolveRound 可读写它。
   * 猜数/投票这类无状态游戏不实现此方法。
   */
  initMatchState?(def: GameDefinition, seatCount: number): unknown;
}
```

**改动要点**：`payload` 和 `initMatchState` 都做成可选，
这样现有两个模板一行不用改。这是向后兼容的关键。

### 赛道与格子设计

100 格太长，建议 **60 格**。理由：每轮平均前进 4–6 格，
60 格约 12 轮结束，而 100 格要 20 轮以上，观众会失去耐心。

格子类型（每种占比）：

| 格子 | 占比 | 效果 | 戏剧性来源 |
|---|---|---|---|
| 平地 | 45% | 无 | — |
| 逆风 | 12% | 下轮前进减半 | 领先者被拖慢 |
| 沼泽 | 10% | 飞行免疫，陆行停一轮 | **让动物选择产生分化** |
| 顺流 | 10% | 额外前进 3 格 | 落后者反超的机会 |
| 迷雾 | 8% | 本轮所有技能卡对你无效 | 保护落后者 |
| 分岔 | 7% | 选近路（有风险）或远路（安全） | 决策点 |
| 雷云 | 5% | 飞行单位被击落，退 5 格 | **克制飞行的关键** |
| 秘藏 | 3% | 抽一张稀有技能卡 | 奖励探索 |

**沼泽与雷云是设计核心**：它们让「选什么动物」这个开局决策
在中盘产生后果。飞行强但怕雷云，陆行慢但不怕沼泽。

### 六种动物（= 六种流派）

| 动物 | 移动方式 | 特性 | 克制关系 |
|---|---|---|---|
| 鹤 | 飞行 | 免疫沼泽/逆风 | 怕雷云 |
| 狼 | 陆行 | 可抢夺他人技能卡 | 怕迷雾（抢不到） |
| 龟 | 陆行 | 免疫所有负面格子 | 速度最慢 |
| 狐 | 陆行 | 可伪装位置（显示假坐标） | 被识破则罚退 |
| 鲤 | 水行 | 沼泽/顺流加速 | 平地最慢 |
| 猿 | 陆行 | 分岔必走近路且无风险 | 无特殊抗性 |

**克制关系要成环，不能有最优解**。上表的环是：
鹤压制陆行 → 雷云压制鹤 → 龟免疫雷云 → 龟太慢被狐骗过 → 狐怕狼抢卡 → 狼怕迷雾。

### 技能卡（每种动物 8 张，共 48 张）

分四类，每类的作用不同：

- **推进类**（3 张/动物）：自己前进
- **干扰类**（2 张）：让他人减速或改道
- **防护类**（2 张）：免疫下一次干扰
- **反制类**（1 张）：抵消并反弹一次干扰

**反制卡只有一张**是刻意的：它让「要不要现在用防护」成为真决策。

### Jev 在这里做什么

你提的思路——LLM 先产生选择，嵌入 Jev 判断——我认为应该**反过来**，
理由是延迟：

```
错误顺序：LLM 生成候选（数秒）→ Jev 判断（300ms）→ 出牌
          总延迟数秒，12 轮就是半分钟等待

正确顺序：本地枚举合法出牌（0ms）→ Jev 从候选中择一（300ms）→ 出牌
          总延迟 300ms
```

Jev 的 `choice` 原语正是为「从预定义候选中选一个」设计的，
而合法出牌集合本来就能由规则枚举出来——不需要 LLM 生成。

具体调用（一次请求拿全三个判断）：

```
state:  当前赛道态势（各马位置、状态、手牌数、剩余格数）
questions:
  play      choice  从 N 张合法手牌中择一（N ≤ 8，够用）
  target    choice  对谁用（仅干扰类卡需要）
  urgency   score   当前局势的紧迫度 0–2（决定是否留牌）
```

实测参考：我验证过 Jev 的三问一包约 300–750ms，
且判别力清晰（空泛输入给低分，实质输入给高分）。

**LLM 的位置在哪**：赛前的「战术宣言」。让玩家的 Agent 用 LLM
写一句开场宣言（「我要用鹤抢跑，谁拦我就反制」），
这句话公开可见，成为其他 AI 的先验信息。
这是慢思考该做的事——**制造信息，而不是做每轮决策**。

### 档位

`mixed-required`，最少 2 人 + 2 Agent。理由：人的非理性出牌
（比如明知不划算也要报复某人）会让赛道局势产生 AI 算不到的变化，
这正是混合对抗的价值。

---

## 四、悬疑感怎么实现

小说的悬疑感来自三件事，都可以机制化：

### 1. 信息不对称（已有基础设施 ✅）

`contracts/matchLog.ts` 的 `secret` 标记 + `projectEvents` 投影已经做到：
座位视角看不到密态事件，观战全知视角能看到。

**要加的是「观众知道而玩家不知道」的演出**。这是悬疑的核心手法：
观战页显示狼人的夜间刀法，而场上玩家还在猜。观众会替玩家着急——
这种张力比单纯的推理更有戏剧性。

实现：观战页读 `SPECTATOR` 投影，用不同视觉层级呈现——
公开信息正常显示，密态信息用**暗红描边 + 低透明度**，
表示"只有你（观众）看得见"。

### 2. 逐步揭示（时间控制）

不要一次性展示结算，要**分步**。现有 `spire/types.ts` 的
`endPlayerTurn` 返回 `Step[]` 就是这个思路，可以推广：

```
揭晓时的节奏（每步 400–600ms）
1. 全员出牌背面朝上落定       ← 悬念峰值
2. 逐个翻开（按提交顺序）      ← 逐步揭示
3. 结算效果（飘字 + 位移）     ← 因果显现
4. 新局势定格                  ← 消化
```

**第 1 步的停顿是悬疑的来源**。现在牌桌直接显示结果，
少了这个「都交了但还没翻」的瞬间。

### 3. 不可靠的信息源

狐的伪装位置、百晓生的「贩声」异能（投放真假情报）——
这些让玩家收到的信息可能是假的。

**这是最像原著的机制**：你以为看清了，但你看到的是别人想让你看到的。

已有异能契约支持（`onProject` 钩子改写投影），缺的是实际的
UI 呈现：假信息应该和真信息**长得一样**，否则就没意义了。

---

## 五、画面特效怎么做

现有技术栈已经够了，不需要引入新库：

```
GSAP           时间线编排（分步演出的骨架）
framer-motion  组件进出场、布局动画
CSS 变量       已有三条动效曲线
```

### 已有的可复用资产

`src/index.css` 里：

- `--ease-ink`（墨滴入水）常规过渡
- `--ease-snap`（快进快出）卡牌翻转
- `--ease-spring`（回弹）奖励飞入
- `.gold-sweep` 金箔光扫
- `.seal-stamp` 朱砂印章

`src/components/game/` 里已有 `RewardFly`、`PhaseBanner`、`LogStream`，
`src/components/spire/combat/` 有整套战斗演出组件。

### 赛马需要新增的特效

| 特效 | 实现 | 优先级 |
|---|---|---|
| 赛道横向滚动 | GSAP 时间线 + transform | P0 |
| 位移飘字（+4 格） | 复用 `RewardFly` | P0 |
| 技能卡飞出与命中 | framer-motion layout 动画 | P0 |
| 雷云击落（鹤） | 竖直下坠 + 屏幕闪白 + 抖动 | P1 |
| 沼泽拖拽（陆行） | 慢速 + 泥浆遮罩 | P1 |
| 狐的伪装揭穿 | 假位置虚化消散，真位置浮现 | P1 |
| 反制卡弹回 | 卡牌镜面翻转后原路返回 | P2 |

### 一条重要纪律

**动效必须能被 `prefers-reduced-motion` 覆盖**。
现有 CSS 已有这条规则（把动画压到 0.01ms），新增特效别绕过它。

这不只是无障碍要求——**演示时如果设备卡顿，你需要一个降级开关**。

---

## 六、爬塔与房间搭建

### 爬塔：建议二选一，不要两套并存

现在的状态（两套爬塔互不相通）是技术债。两条路：

**路线 A（推荐，成本低）**：把契约层的塔当作「主线」，
现有 `src/engine/spire` 当作「塔内战斗」。

也就是：`FloorDef` 决定这一层的门禁与主场花色，进门后跑的是
现有的类尖塔战斗。需要写 48 层数据文件（约 200 行），
把 `checkGate` 接到 `SpireHub` 的入口判断。

**路线 B（成本高）**：放弃契约层的 48 层设计，
只保留现有单人爬塔。但这样就丢掉了「残章在 A 层掉、B 层才有用」
那套留存设计，可惜。

### 48 层数据怎么写（路线 A）

```ts
// contracts/floors.data.ts（待建）
export const FLOORS: FloorDef[] = [
  { floor: 1,  tier: "huang", zodiacIndex: 0, name: "鼠洞",
    suit: "club", gate: { kind: "open" }, rulebookId: "rb-guess", isBoss: false },
  // ...
  { floor: 12, tier: "huang", zodiacIndex: 11, name: "豕圈",
    suit: "club", gate: { kind: "relicSet", setId: "set-club-1" },
    rulebookId: "rb-guess", isBoss: true },
  // 第 25–36 层门禁用 { kind: "anyRuling" }——须质询成功过
  // 第 37–48 层用 { kind: "authoredGame" }——须有自创游戏被收录
];
```

**门禁的递进是整个游戏的骨架**：
学会看（集残章）→ 学会骗（跨花色）→ **学会读规则（质询）**
→ **学会改规则（出题）**。

第 25 层那道 `anyRuling` 门是最妙的一笔：它强制玩家至少
质询成功一次，否则上不去。这让裁判机制从"可选玩法"变成"主线必经"。

### 房间搭建要补的

| 缺口 | 现状 | 要做 |
|---|---|---|
| 席位策略选择 | 无 | 创建房间时选 agent-only / 混合 |
| 真观战 | 按钮跳新对局 | 接 `world.matchEvents` |
| 房间列表筛选 | 无 | 按策略/游戏/位阶筛 |
| 观战人数显示 | 无 | 让房主知道有人在看 |

**席位策略选择是必须的**——我已在契约层加了 `SeatPolicy`
（三档 + 13 项测试），但创建房间的 UI 还没有对应选项。

---

## 七、建议的执行顺序

前两步有依赖，不能跳：

1. **契约层扩展**（半天）：`play` 动作 + `payload` + `initMatchState`
   这是赛马的前提，且对未来所有复杂游戏有用
2. **超能力赛马**（两天）：六动物 + 48 卡 + 60 格赛道 + Jev 决策
3. 并行：
   - 海盗分金（规则书已写好，一天）
   - 达芬奇密码 + 概率热力图（一天半，观赏性高）
   - 千轮猜数（半天，改参数即可）
   - 48 层数据 + 门禁接线（一天）

### 优先级判断

如果时间只够做两个新游戏，做**赛马 + 达芬奇密码**：

- 赛马提供**视觉冲击**（因果链、动物互动、赛道演出）
- 达芬奇密码提供**智力深度**（概率热力图让 AI 的推理可见）

两者互补，一个抓眼球一个抓脑子。海盗分金虽然规则书已写好，
但它的观赏性依赖自然语言谈判，72 小时内做不出好效果。
