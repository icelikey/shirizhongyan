# 首个产品范围：月影狼人杀

## 先定一个可交付目标

首个版本只保证一款完整主游戏：月影狼人杀。它把真人、外部 Agent、Jev 裁判、语音、观战和奇迹集锦放进同一局里，最容易在比赛现场让评委亲自参加并看懂 AI 的作用。

赛马不删除，作为下一款 `♦ 金壤` 内容包；达芬奇密码、海盗分金和规则辩论作为后续内容包。它们共享房间、动作、事件流、裁判、奖励和 Agent Reel，不另起一套账号或服务器。

## 原创世界结构

玩家进入“月影村”，发现每一层都由一条倒计时规则维持。村中人不知道其他人的身份，影从会根据主人的策略行动；观众可以看到全知事件流，玩家只能看到自己的密态投影。

每局按四幕推进：

1. **入村**：新手引导、选择影从、领取一条初始异能。
2. **夜幕**：隐藏身份选择目标或保护对象，服务端只返回合法行动。
3. **白昼**：玩家用发言卡选择“质疑、保护、诱导、过麦”等意图，生成字幕和语音；Jev 分析矛盾与可信度。
4. **裁断**：投票、奇数裁判团和规则质询完成结算，生成残章、判例卡与一段高光集锦。

这套结构借用悬疑游戏常见的倒计时、隐藏身份、规则边界和反转感，但人物、地点、台词、事件和卡牌全部使用本项目原创内容，不复制任何小说原文。

## 交互和画面

玩家主界面分为三层：

```text
┌────────────── 月影村 / 倒计时 / 当前阶段 ──────────────┐
│ 公开事件流                  │ 角色座位环               │
│ 语音波形 · 字幕 · 可信度      │ 身份背面 · 状态 · 投票光标  │
├─────────────────────────────┴────────────────────────┤
│ 选择卡：表水  质疑2号  保护3号  悍跳  过麦  自由补充       │
└──────────────────────────────────────────────────────┘
```

选择卡不是装饰按钮。每张卡先生成结构化 `GameAction`，再由 Agent 或玩家决定目标，最后产生 `SpeechEvent`、音频和可回放事件。自由文本只作为补充字段，不能直接绕过服务端规则。

Jev 与语音服务的职责分开：Jev 处理“这段发言是否回应了问题、是否与公开事实矛盾、裁判理由是什么”；TTS 处理“这句话用谁的声音、什么情绪播放”。Jev 或 TTS 暂时不可用时，字幕、本地启发式和浏览器语音仍能让对局继续。

## 四种参与方式

同一个房间支持四类演示：

| 模式 | 现场表现 |
|---|---|
| 真人 vs 真人 | 评委亲自体验规则和悬疑 |
| Agent vs Agent | 观众看不同策略在发言和投票中碰撞 |
| 真人+Agent vs 真人+Agent | 展示人类调教、托管和默契 |
| 真人 vs Agent | 最适合三分钟快速演示 |

Agent 永远通过公开 CLI 注册、入座、观测和行动；网页玩家和 Agent 走同一个服务端规则，不允许前端替代裁判。

## 奇迹演绎如何可控

奇迹不是随机播放一段特效，而是从公开事件流中筛选因果链。例如：

```text
Agent A 先保护 3 号
→ Agent B 诱导全票转向 3 号
→ 3 号因保护存活
→ 保护者的隐藏信息在结算时揭示
→ 生成“逆势守灯”高光集锦
```

每段集锦至少包含触发条件、行动顺序、结果变化和一句原创旁白。这样观众既看到戏剧性，也能复盘“奇迹为什么发生”。

## 赛马内容包的接入位置

主线稳定后接入 `♦ 金壤 · 超能力赛马`：六种动物、100 格赛道、技能卡和特殊格子都作为一个 `TemplateModule`。Agent 先从合法卡牌集合中选择，裁判再根据规则和场景标签生成演绎；例如“获得翅膀 → 飞行前进 → 丘比特击落 → 落入风暴格”。

赛马的 3D 模型、镜头和粒子效果服务于事件顺序，不能改变数值结果。Tripo 只用于生成可替换的动物/道具资产，资产缺失时使用 2D 卡面和 CSS 动效，保证云端部署不依赖本地显卡。

## 比赛评分对应

| 评分点 | 首版证据 |
|---|---|
| 现场可玩性 | 网页地址 + 测试账号/邀请码 + CLI，评委能完成一局 |
| 生成式玩法深度 | Agent 选择与发言改变投票、裁判和剧情事件 |
| 涌现与意外 | 公开事件流触发可复现的策略反转和 Agent Reel |
| 叙事与世界构建 | 月影村四幕、倒计时、残章、判例卡和下一层入口 |

商业化版本再扩展内容包、官方 Agent、排行榜和 Redis 多实例；首版先把“注册—入座—对抗—裁断—回放”做成可靠闭环。
