三人分工任务栏（含验收标准）

**直接发给组员。** 每张卡含：做什么、为什么、怎么算做完。
验收标准写成可执行的检查项——**不是「做得好看」这类没法判定的话**。

---

## 零、所有人开工前（15 分钟）

### 三条铁律（破了要重新平衡全局）

出自 `contracts/` 三个文件，这是架构能横向加游戏的原因：

1. **卡牌不参与胜负计算**（`cards.ts`）
   卡决定能解开什么、进入哪里、引用什么条款，**不决定伤害数值**。
2. **异能只改「信息」与「时间」**（`ability.ts`）
   加时、多看、看歪、改手都行；改胜负、改计分不行。
3. **密态事件只出现在全知投影**（`matchLog.ts`）
   新增密态事件打 `secret: true` 即自动受保护。

**每次提交前自问**：我破了哪一条吗？

### 环境（照做即可）

```bash
git clone https://github.com/icelikey/shirizhongyan.git
cd shirizhongyan/app
pnpm install
cp .env.example .env
```

`.env` 里 Kimi 两个 URL **必须填合法地址**，否则后端 `new URL("")` 崩溃、
大厅全报 500：

```env
KIMI_AUTH_URL=https://auth.kimi.com
KIMI_OPEN_URL=https://open.kimi.com
VITE_KIMI_AUTH_URL=https://auth.kimi.com
```

数据库**必须 MySQL 8**（MariaDB 10.4 不认 `serial AUTO_INCREMENT`）：

```bash
docker run -d --name tdg-mysql -p 3306:3306 \
  -e MYSQL_ALLOW_EMPTY_PASSWORD=yes \
  -e MYSQL_DATABASE=ten_days_gambit mysql:8
pnpm db:push
pnpm tsx db/seed.ts      # 造六个演示账号，输出六把 Agent Key
pnpm dev
```

### 通用验收门槛（每张卡都要过）

```bash
pnpm check                                  # tsc -b，必须 0 错误
npx vitest run contracts/ api/world/        # 必须全绿（当前 184 项）
```

**这两条不过就不许推 main。** 没有例外。

---

## 一、A 号（你）· 规则内核与裁判

### A1 · 事件流落库 ✅ 已完成

已打通「对局 → 事件流 → 落库 → 彩蛋 → 发卡」，19 项测试。

### A2 · 辩论游戏（♠ 玄渊）

**为什么必须做**：评分标准要求「AI 真正驱动玩法循环，而非贴皮」。
而能证明 AI 不可替代的只有**模糊议题**——确定性规则用算法就够了。
辩论是模糊议题最好的载体。

前提已就绪：`contracts/room.ts` 的 `speak` 动作（文本提交，上限 600 字）。

**要做**：
1. `contracts/rulebooks.data.ts` 加 `RB_DEBATE`（条款分类要含 `conduct`）
2. `api/games/sdk/debate.ts` 实现 `TemplateModule`
3. 裁判用 `api/world/jevJudge.ts` 的模式：Jev 三问一包，无 Key 时降级

流程：立论 → 交叉质询 → 结辩 → 3 席裁判投票。

**验收标准**：
- [ ] 2v2 能跑完，产出裁决与理由
- [ ] Jev 不可用时能降级，且降级路径有测试覆盖
- [ ] 裁判投票数恒为奇数（3/5/7），无平票分支
- [ ] `pnpm check` 0 错误，新增至少 8 项测试

### A3 · 48 层爬塔数据

`contracts/spire.ts` 的 48 层谜题链设计完整，但**数据文件不存在**
（`FloorDef` 在 `src` 下零引用）。

门禁递进本身就是叙事：
```
学会看（集残章）→ 学会骗（跨花色）→ 学会读规则（须质询成功过）→ 学会改规则（须有自创游戏）
```

第 25 层那道 `anyRuling` 门是最妙的一笔——**它让裁判机制从可选玩法变成主线必经**。

**验收标准**：
- [ ] `contracts/floors.data.ts` 含 48 层，每层有名、花色、门禁、规则书
- [ ] 第 1–12 层门禁为 `open` 或 `relicSet`，第 25 层起含 `anyRuling`，
      第 37 层起含 `authoredGame`
- [ ] `checkGate` 接到 `SpireHub` 入口，未达门禁显示世界观提示而非报错
- [ ] 层名不用「第 N 层」这类占位，用生肖 + 风物（如「鼠洞」「豕圈」）

---

## 二、B 号 · 小游戏

**天然零冲突**：游戏模板走 `TemplateModule` 接口，一人一文件。

**不要碰**：`api/world/`、`api/games/sdk/runtime.ts`、`src/index.css`。

### B1 · 超能力赛马（优先级最高）

**契约与数据已写好，37 项测试已全绿**：
- `contracts/beastRace.ts` — 六兽、八格子、位移计算
- `contracts/beastRace.data.ts` — 48 张技能卡、赛道生成器

你做的是**模板执行器与演出**，规则设计不用重新想。

两条铁律已被测试守住，改数值会立刻失败：克制成环、扰行不强于疾行。

**要做**：
1. `api/games/sdk/beastRace.ts` — `initMatchState` 生成赛道与六匹马，
   `resolveRound` 按「卡牌结算 → 位移 → 格子触发 → 判完赛」顺序
2. 前端赛道页 + 演出

**演出节奏（每步 400–600ms）**：
```
1. 全员出牌背面落定   ← 悬念峰值，现在的牌桌缺这个停顿
2. 逐个翻开（按提交顺序）
3. 结算飘字 + 位移     ← 因果显现
4. 新局势定格
```

**验收标准**：
- [ ] 六人局能跑完，有马冲线即提前终局（`resolveRound` 返回 `finished`）
- [ ] `RaceReveal.effects[]` 能让观众看懂「谁因为什么前进/后退了」
- [ ] 动效受 `prefers-reduced-motion` 约束（现有 CSS 已有规则，别绕过）
- [ ] 37 项契约测试仍全绿，新增至少 10 项模板测试
- [ ] `pnpm check` 0 错误

### B2 · 达芬奇密码（♥ 丹丘）

**为什么值得做**：它是全部候选里最适合 AI 对抗的——信息完全公开且离散，
无自然语言歧义，是**纯贝叶斯推理**。

**杀手级演出：概率热力图。** 观战时显示每个 AI 对每张未知牌的概率估计，
观众能看到 AI 脑中的分布逐轮收缩。**桌游原版做不到这个，请当必做项。**

规则：黑白各 0–11 共 24 张，每人 4 张自己不可见、他人可见，
手牌须升序排列（同数黑在白前），轮流猜「某人第 N 张是某数」。

**升序约束是算法核心**：若第 2 张已知是 5，则第 1 张必 < 5、第 3 张必 > 5。

**验收标准**：
- [ ] 四人局能跑完
- [ ] 概率函数是纯函数且有至少 10 项测试，覆盖升序约束的推论
- [ ] 热力图随每轮翻牌更新，数值与概率函数输出一致
- [ ] `pnpm check` 0 错误

### B3 · 演出判定器扩充 14 → 40 条

**这张卡直接决定「奇迹密度」。** 数学是：单条 8% 触发率，
6 条只有 39% 命中，40 条达 96%。`api/world/spectacle.test.ts` 已验算。

现有 14 条在 `api/world/spectacle.ts`，分六类（逆转/精算/博弈/规则/异能/巧合）。

**写判定的三条纪律**：
1. 只读**公开可见**的过程量——观众看不懂的判定没有演出价值
2. `caption` 必须说清「为什么这值得看」，不能只说「发生了什么」
3. 条件要能从事件流直接算出，不引入新的状态跟踪

**验收标准**：
- [ ] 判定器 ≥ 40 条，id 唯一
- [ ] 每条的 `caption` 不少于 8 字且说明了价值
- [ ] 全部判定器在空事件流上不抛错
- [ ] `atLeastOneRate(0.08, 条数)` ≥ 0.95（测试里已有这个断言）
- [ ] 六类各不少于 5 条（避免只堆容易写的那类）

### B4 · 千轮猜数（agent-only）

价值是**做人类做不到的事**：观众看 level-k 收敛曲线从散布收缩到均衡附近。

**建议至少做一个 agent-only 游戏**，否则「AI vs AI」只是把人换成 AI。

大部分是改参数：`SDK_LIMITS` 的 `rounds.max` 放到 1000、
`submitWindowSec.min` 放到 1，加官方定义 `seatPolicy: "agent-only"`。

**验收标准**：
- [ ] 200 轮能跑完且前端不卡（**不要每轮重渲染整张图**）
- [ ] 收敛曲线用 recharts（已在依赖里），能看出趋势
- [ ] agent-only 房由 0 号 Agent 席开局（`runtime.ts` 已支持）
- [ ] `pnpm check` 0 错误

---

## 三、C 号 · 美术与前端

**不要碰**：`api/` 下任何文件、`db/schema.ts`。

**`src/index.css` 只有你能改**——避免色彩令牌被覆盖。

### 先读硬约束（`DESIGN.md` 第八节有完整七条）

最关键三条：

1. **暗色唯一**，不引入亮色模式，不改 body 的星云 + 噪点两层
2. **四花色语义色固定**：♠`#8B93F8` ♥`#EE6A72` ♣`#4ECB9C` ♦`#F2A93B`
   且 `src/index.css` 与 `src/data/echoes.ts` **两处必须同步**
3. 新色值一律用 CSS 变量定义在 `:root`，不写死在组件里

### C1 · 观战页 + 裁判席 UI（优先级最高）

**为什么最重要**：评分标准「观众可围观」是明文要求，
而裁判机制是本作最大原创性——**它现在只存在于测试里，评委看不见**。

后端已完全就绪：`world.matchEvents`（按视角投影取事件流）、
`world.appeal`（返回裁判投票与 `jevMeta` 含每席耗时）、`world.recentRulings`。

**悬疑感的实现手法：观众知道而玩家不知道。**
观战页读 `SPECTATOR` 全知投影，能看到密态事件（狼人刀法、真实身份）。
视觉要分层：公开信息正常显示，**密态信息用暗红描边 + 低透明度**，
表示「只有你（观众）看得见」。观众会替玩家着急——这比单纯推理更有戏剧性。

裁判席三席（或五、七）各一枚徽记，投票时**逐个亮起**。
每席显示：名号、类型（J0 算律 / J1 明镜 / J2 众议）、投票、理由一句。
**把 Jev 的 300–700ms 延迟显示出来**——这是「快判断」的证据。

**验收标准**：
- [ ] 能回放一局完整对局，事件按 `seq` 顺序演出
- [ ] 密态信息与公开信息视觉上可区分（截图能说明差异）
- [ ] 裁判投票逐席亮起，非一次性全显
- [ ] 质询面板只列 `appealable: true` 的条款
- [ ] 大厅「观战」按钮接真房间（**现在是跳到新对局，是假的**）
- [ ] `pnpm check` 0 错误

### C2 · 奇迹集锦播放器

`contracts/spectacle.ts` 已定义好 `Highlight` 与六种镜头语言，
`api/world/spectacle.ts` 产出片段。你做播放。

六种镜头：
| kind | 含义 | 建议实现 |
|---|---|---|
| `freeze` | 定格放大 | scale + 边缘发光 + 时间暂停感 |
| `slowmo` | 慢放 | GSAP timeScale 降到 0.3 |
| `track` | 追焦 | 镜头跟随某座位，其余压暗 |
| `split` | 分屏对比 | 两座位并列，中间金线分隔 |
| `rewind` | 回溯 | 倒放到 `foreshadowSeq` 再快进回来 |
| `wide` | 全场 | 拉远看整体局势 |

**时长由稀有度决定**（`rarityMeta(rarity).durationMs`，900–4000ms），
不要自己定。

**验收标准**：
- [ ] 六种镜头都有对应动画，视觉上可区分
- [ ] 播放顺序按 `anchorSeq`（挑选看稀有度，**播放看时序**）
- [ ] 稀有度越高演出越隆重（mythic 要明显不同于 common）
- [ ] 可跳过（总时长可能达 15 秒）
- [ ] 动效受 `prefers-reduced-motion` 约束
- [ ] `pnpm check` 0 错误

### C3 · 待补素材

| 优先级 | 素材 | 数量 | 用途 |
|---|---|---|---|
| P0 | 裁判席徽记 | 3 | J0 算律 / J1 明镜 / J2 众议 |
| P0 | 判例卡卡面 | 1 | judicial 唯一稀有度，需明显区别于残章卡 |
| P0 | 稀有度框 | 5 | common → mythic，集锦浮层用 |
| P1 | 六兽立绘 | 6 | 赛马（鹤狼龟狐鲤猿） |
| P1 | 观战页幕布 | 2 | 舞台感 |

**素材纪律**：
- 命名 `{类别}-{标识}.{png|svg}`，小写连字符，**不用中文文件名**
- 图标几何图形用 SVG，立绘背景用 PNG
- **入库前必须跑压缩**：`node scripts/compress-assets.mjs`
  （已把 183MB 压到 81MB，别让它涨回去）

**验收标准**：
- [ ] 全部素材已压缩（跑 `--dry` 确认无可压缩项）
- [ ] 命名符合规范，无中文文件名
- [ ] 五档稀有度框视觉层次分明（mythic 一眼看出比 common 贵重）

### C4 · 监控面板

评分标准「产品完成度」明文要求「**含监控/可视化界面**」，容易漏。

数据都已就绪：`world.recentRulings`、`world.recentMatches`、
质询返回的 `jevMeta`。

**要显示**：在线房间与席位构成（人/Agent/bot 各几席）、最近判例
（规则演化的可视证据）、Jev 调用延迟分布。

**验收标准**：
- [ ] 三块数据都显示且来自真实接口，无硬编码
- [ ] 席位构成能看出「这局有几个真 Agent」
- [ ] `pnpm check` 0 错误

### C5 · 新手引导实现

`contracts/onboarding.data.ts` 九步已写好（路由驱动，非 CSS 选择器）。
末步落在「规则可被质询」——**全作唯一「玩家能改变世界」的机制**。

**验收标准**：
- [ ] 九步能走完，进度存 localStorage
- [ ] `anchor` 为 null 或找不到元素时**降级居中浮层**，不报错不飘角落
- [ ] 跳过后不再弹出，`ONBOARDING_VERSION` 变更时重新引导
- [ ] `pnpm check` 0 错误

---

## 四、文件归属（防冲突）

| 路径 | 归属 | 规则 |
|---|---|---|
| `contracts/` | **共有** | 改前群里说一声，这是三方共享地基 |
| `api/world/` | A | |
| `api/games/sdk/runtime.ts` | A | B 加模板不要改这个文件 |
| `api/games/sdk/<你的游戏>.ts` | B | 一人一文件，零冲突 |
| `src/pages/` `src/components/` | C | |
| `src/index.css` | **只有 C** | 避免色彩令牌被覆盖 |
| `db/schema.ts` | A | 改表结构必须通知，其他人要 `pnpm db:push` |

### `contracts/` 的纪律

唯一高冲突区。规则：

1. **只增不改**：加新类型、加可选字段随意；改字段名或删字段 → 群里确认
2. 一人一文件，不交叉
3. 改完立刻 `pnpm check` + 推送，别攒着

---

## 五、Git 流程

```bash
git checkout main
git pull --rebase origin main        # 先同步，避免分叉
git checkout -b feat/beast-race      # 前缀：feat/ fix/ art/

# 小步提交
git add -A && git commit -m "feat: 赛马模板执行器"

pnpm check                           # 0 错误才能推
git push -u origin feat/beast-race
# 开 PR → 另一人瞄一眼 → 合并
```

**三条铁律**：
1. `main` 永远可跑——推前必须 `pnpm check` 通过
2. 用 `pull --rebase` 而非 `pull`——历史线性，冲突好解
3. 每天至少推一次——攒代码是 72 小时比赛最大的风险

### AI 生成的代码必须有人过目

这条别省。我让 codex 生成新手引导文案，文案写得好，
但它**虚构了九个 CSS 选择器**（项目里 grep 零命中），引导会指向空气。
两分钟 review 就能发现。

---

## 六、排期

### 第一天

| 谁 | 做什么 |
|---|---|
| A | A2 辩论游戏 |
| B | B1 赛马模板 |
| C | C1 观战页骨架 |

**当天结束全员合并一次**，确认 main 能跑通一局完整对局。

### 第二天

| 谁 | 做什么 |
|---|---|
| A | A3 48 层数据 + 门禁接线 |
| B | B3 判定器扩充到 40 条（这个决定奇迹密度） |
| C | C1 裁判席 + C2 集锦播放器 |

**当天结束再合并 + 端到端演练。**

### 第三天

上午：B2 或 B4 二选一、C4 监控面板、C3 素材补齐
下午：**冻结代码**，只修致命 bug
晚上：排练演示 + 录屏备份

### 两件小事别忘

- **字体自托管**（30 分钟）：现在走 Google Fonts CDN，
  现场断网则印章字回退系统字体，观感明显变差
- **录屏备份**（一小时）：网络或数据库故障是常见事故

---

## 七、验收总表

任何一张卡合并前，这五项必须全过：

| 检查 | 命令 | 标准 |
|---|---|---|
| 类型 | `pnpm check` | 0 错误 |
| 测试 | `npx vitest run contracts/ api/world/` | 全绿 |
| 铁律 | 人工自查 | 没破三条 |
| 无障碍 | 人工自查 | 动效受 `prefers-reduced-motion` 约束 |
| 素材 | `node scripts/compress-assets.mjs --dry` | 无可压缩项 |

**这张表贴在群里。** 合并前对一遍，比事后返工省时间。
