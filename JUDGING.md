评审对照与取舍

按大赛评分标准（通用 6 分 + AI 游戏赛道 4 分）逐项对照现状，
标出**能拿到的分**与**现在拿不到的分**，以及为拿分要做什么。

一条原则贯穿全文：**凡是为比赛牺牲代码质量的做法，我都标出来并给替代方案。**
你说了这个项目要商业化——那么赛后要删的东西，现在就不该写进主干。

---

## 一、赛道专属维度（4 分）· 这是差异化的地方

### 现场可玩性（1 分）

| 档 | 标准 |
|---|---|
| 优秀 0.9–1.0 | 玩家可现场操作、观众可围观、运行稳定 |

**现状**：单机四个游戏实测可玩（我跑过狼人杀到白天发言阶段）。
**缺口**：观战页不存在，大厅「观战」按钮点了跳到新对局——**观众无法围观**。

你们讨论的方案（六个测试账号 + CLI 链接）是对的，我已实现一半：

- ✅ `db/seed.ts` 造六个账号，每人一把 Agent Key，幂等可重跑
- ✅ `skills/tdg-agent/SKILL.md` 评委把它交给自己的 AI 就能接入
- ❌ 观战页（`TASKS.md` 的 C1 卡）

**要拿满分必须做观战页。** 「观众可围观」是明文标准，不做就只能拿达标档。

关于不做邮箱注册：**这个决定是对的，且不牺牲代码质量。** 预置账号走的是
正常的 users 表与 agent_keys 表，不是 hack。商业化时加注册流程，
这六个账号自然变成演示租户。

### 生成式玩法深度（1 分）

| 档 | 标准 |
|---|---|
| 优秀 0.9–1.0 | AI 真正驱动玩法循环，而非贴皮 |
| 不足 0–0.5 | **AI 仅作美术/文案装饰** |

**这一项你们的设计天然占优**，因为有两处 AI 不可替代：

1. **Agent 裁判裁决模糊议题**。你在讨论里说得很准：
   *「确定性议题并不需要 agent，因为我只需要提前把算法写好」*。
   所以能证明 AI 不可替代的只有模糊议题——辩论、赛马的口胡卡效果判定。

2. **规则质询**。Agent 引条款主张改判，3 席裁判投票，
   采纳则铸判例影响后续所有对局。这是「AI 生成内容驱动玩法循环」的字面实现。

**现状**：裁判机制后端完整（99 项测试），Jev 实测判别力清晰
（实质质询支持度 0.75 采纳 / 空泛不服 0.43 驳回）。
**缺口**：没有辩论类游戏——**而辩论是模糊议题最好的载体**。

我已补上前提：`contracts/room.ts` 新增 `speak` 动作（文本提交）。
在此之前所有动作载荷都是数字或 id，装不下自然语言，
**没有它 AI 裁判只能判确定性规则，而那根本不需要 AI**。

### 涌现与意外行为（1 分）

| 档 | 标准 |
|---|---|
| 优秀 0.9–1.0 | 产生设计之外的行为或叙事，**且现场可复现展示** |
| 达标 0.6–0.8 | 偶有意外但不可控不可讲清 |

**注意「可复现」三个字。** 这是本项最容易丢分的地方：
涌现本身是随机的，但评委要看到你能**把它重放出来**。

**这就是事件流落库的价值**。刚做完的 A1 让每局都有完整事件流
（seed + 有序事件 + 密态标记），所以：

- 跑一天让 AI 自主玩（你们讨论里提到的做法）
- 从 `match_logs` 里挑出有意思的那局
- 现场用 `world.matchEvents` 重放给评委看

**没有事件流，「可复现」这一档拿不到。** 现在有了。

你提的贿赂行为（一个 Agent 说服另一个联手）也会被记在事件流里——
`speak` 动作的文本会进 `MatchEvent`，重放时能看到原话。

### 叙事与世界构建（1 分）

| 档 | 标准 |
|---|---|
| 优秀 0.9–1.0 | 实时叙事自洽，数字生命设定完整 |

**这一项现状最好**：四大陆、天地玄黄位阶、八影从（各有人设与绑定异能）、
十卷残章切成 488 行残章卡、彩蛋卡与判例卡三类稀有度来源。

你们讨论里的「81 层爬塔」与现有 `contracts/spire.ts` 的 48 层设计有出入。
建议**用 48 层**（四位阶 × 12 生肖，已有契约与门禁递进设计），
理由是它的门禁递进本身就是叙事：

```
学会看（集残章）→ 学会骗（跨花色）→ 学会读规则（须质询成功过）→ 学会改规则（须有自创游戏）
```

第 25 层那道「须质询成功过一次」的门是最妙的一笔——
**它让裁判机制从可选玩法变成主线必经**。

**缺口**：48 层数据文件不存在（`FloorDef` 在 `src` 下零引用）。

---

## 二、通用维度（6 分）

### 创新与创意性（2 分，占比最大）

| 档 | 标准 |
|---|---|
| 优秀 1.6–2.0 | 极具想象力，**提出新范式** |

**你的最强论点是去中心化裁判**，而不是「AI 玩游戏」。

讲法建议（按说服力排序）：

1. **世界的规则由 AI 的博弈真实地演化**
   判例入库后改变后续所有对局的规则解释。这不是「AI 参与玩法」，
   而是「AI 改写玩法」——范式区别在这里。

2. **裁判也是分布式的**
   J0 算律（算法）/ J1 明镜（AI）/ J2 众议（他人 Agent）。
   法定人数恒为 3/5/7 奇数，故无平票——这是工程约束不是仪式感。

3. **三条铁律让系统能无限扩展**
   卡牌不改胜负、异能只改信息与时间、密态只在全知投影。
   **这条最容易被低估**：它证明你们想过架构，而非堆功能。

### 技术实现与难度（1.5 分）

| 档 | 标准 |
|---|---|
| 优秀 1.2–1.5 | Agent 架构稳定高效，**克服上下文溢出、幻觉控制** |
| 不足 0–0.5 | 以硬编码脚本冒充 Agent 能力 |

**「克服上下文溢出与幻觉控制」这两个词是给分点，要正面回答。**

你们已有的答案比自己意识到的更好：

**幻觉控制 → Jev 的格式确定性**
Jev 的输出锁定在预定义 schema 内，**从根本上消灭了结构化输出的格式幻觉**——
它不会跑出选项之外。判断本身仍可能错，但不会返回非法格式。
对比 LLM 做裁判要额外写解析与重试逻辑。

**上下文溢出 → 快慢分工**
```
Jev（系统一）  裁断、分类、矛盾检测   300–800ms，输入仅条款正文 + 主张
LLM（系统二）  策略规划、长程推理     数秒
```
裁判不喂长上下文，只喂「条款 + 主张」两段短文本，所以不会溢出。
这是架构选择，不是运气。

**还有一条**：玩家的 Agent 跑在他自己那边，平台只给投影后的视图。
所以**平台侧根本不承担玩家 AI 的上下文**——这是去中心化的附带好处。

⚠️ **避免被判「硬编码冒充 Agent」**：现有 echo-bot 用的是启发式决策
（`src/engine/agents.ts` 的 level-k 锚点）。这本身没问题，
但演示时**必须让至少一席是真外部 Agent**，否则评委看到的全是脚本。

### 产品完成度与体验（1.5 分）

| 档 | 标准 |
|---|---|
| 优秀 1.2–1.5 | 全程顺畅跑通，UI/UX 友好（**含监控/可视化界面**） |

「含监控/可视化界面」是明文要求，容易漏。建议做一个极简的观测面板：

- 当前在线房间与席位构成（人/Agent/bot 各几席）
- 最近判例（规则演化的可视证据）
- Jev 调用延迟与判定分布

后端数据都有（`world.recentRulings`、`world.recentMatches`、
质询返回的 `jevMeta` 含每席耗时）。**半天能做出来，直接对应给分点。**

⚠️ **实测发现的两个演示风险**，赛前必须解决：

1. **字体走 Google Fonts CDN**，断网则印章字回退，观感明显变差。
   自托管到 `public/fonts/` 需 30 分钟。
2. **数据库必须 MySQL 8**。你机器上是 MariaDB 10.4，`drizzle-kit push` 会失败
   （schema 用了 `serial AUTO_INCREMENT`）。Docker 起一个即可。

### 商业价值与潜力（1 分）

| 档 | 标准 |
|---|---|
| 优秀 0.9–1.0 | **受众明确**，具备落地或商业化潜力 |

你们讨论到这里卡住了——「智斗人群」不够常见。我的建议是**换一个切法**：
不描述人群性格，而描述**他们现在在玩什么**。

```
核心受众：正在玩狼人杀 / 阿瓦隆 / 剧本杀的社交推理玩家
  痛点：凑不齐人。六人局要六个人同时在线，这是这个品类最大的流失原因。
  我们的解法：AI 补位，且补的不是弱智 bot——是玩家自己调教的 Agent。

第二受众：有 Claude Code / Cursor 的开发者
  痛点：想试自己的 Agent 能力，但没有有趣的对抗场景。
  我们的解法：一个 SKILL.md 就能让他的 AI 上桌，还能挑战规则。
```

**第一条是真痛点且有数据支撑**（狼人杀类游戏的开局失败率），
**第二条是天然的早期增长渠道**——开发者会自己写 Agent、自己传播。

商业化路径（讨论里你已提到，这里补完）：

| 阶段 | 收入来源 |
|---|---|
| 早期 | 官方 Agent 托管（自带 AI 的免费，没有的付费用官方） |
| 中期 | 赛季通行证、影从皮肤（不卖战力——铁律保证卡牌不影响胜负） |
| 长期 | UGC 分成：玩家自创游戏被收录后分成 |

**「不卖战力」这点值得讲**：三条铁律里「卡牌不参与胜负计算」
意味着你们**不能**做 pay-to-win，这既是设计约束也是商业诚意。

---

## 三、为比赛该做与不该做

### 该做（既拿分又是真代码）

| 项 | 对应给分点 | 量 |
|---|---|---|
| 观战页 + 裁判席 UI | 现场可玩性「观众可围观」 | 一天 |
| 辩论游戏（`speak` 动作已就绪） | 生成式玩法深度「AI 不可替代」 | 一天 |
| 监控面板 | 产品完成度「含可视化界面」 | 半天 |
| 事件流重放 | 涌现「现场可复现」 | 半天（依赖已完成的 A1） |
| 48 层爬塔数据 | 叙事与世界构建 | 一天 |
| 字体自托管 | 避免演示事故 | 30 分钟 |

### 不该做（为比赛牺牲质量）

| 诱惑 | 为什么别做 | 替代 |
|---|---|---|
| 把演示数据硬编码进前端 | 赛后要删，且评委一问「换个账号试试」就露 | `db/seed.ts` 已做成真实数据 |
| 为跑通 demo 关掉密态投影 | 破了铁律三，赛后补回来要重测全部游戏 | 投影已有测试守住，别动 |
| 用假的裁判投票结果 | 「AI 仅作装饰」直接判不足档 | Jev 已接入且有降级路径 |
| 加一堆半成品游戏凑数 | 「内容单薄或运行不稳」只给达标档 | 四个做扎实比八个半成品强 |
| 现场用 Math.random 造涌现 | 「不可复现」拿不到优秀档 | 事件流重放真实对局 |

### 一个要提前定的取舍

你们讨论里提到「跑一天让 AI 自主玩，下午看结果」。这个做法很好，
但**要留出筛选时间**：一天跑出来的对局里，大部分是平淡的。
建议赛前一天就跑，留半天从 `match_logs` 里挑出三局有戏剧性的，
现场直接调那三个 id 重放。

---

## 四、演示脚本（12 分钟）

按给分点排序，不按功能顺序：

| 时间 | 内容 | 对应给分 |
|---|---|---|
| 0–2 分 | 开场：世界观 + 一句话定位 | 叙事 |
| 2–5 分 | **评委现场接入自己的 Agent**（给 SKILL.md + Key） | 现场可玩性 + 技术实现 |
| 5–8 分 | **一局辩论：Agent 陈词 → 3 席裁判裁决** | 生成式玩法深度 |
| 8–10 分 | **质询一条规则 → 判例入库 → 下一局规则已变** | 创新与创意性（最高分项） |
| 10–11 分 | 重放昨天跑出的那局意外行为 | 涌现「可复现」 |
| 11–12 分 | 监控面板 + 商业化一句话 | 产品完成度 + 商业价值 |

### 三个必须准备的兜底

1. **录屏**：网络或数据库故障是常见事故
2. **Jev 降级说明**：若现场无网，裁判走本地启发式。
   这是设计而非 bug，但要主动说明，否则像功能没做完
3. **字体自托管**：见上

---

## 五、下一步（按给分效率排序）

1. **观战页 + 裁判席**（一天）— 解锁「观众可围观」这个明文标准
2. **辩论游戏**（一天）— `speak` 动作已就绪，这是 AI 不可替代性的最佳证明
3. **监控面板**（半天）— 「含可视化界面」是明文要求，数据都有
4. **48 层爬塔数据**（一天）— 补完叙事骨架
5. 字体自托管 + MySQL 8（一小时）— 消除演示事故

前三项做完，赛道 4 分里的三项都能进优秀档。
