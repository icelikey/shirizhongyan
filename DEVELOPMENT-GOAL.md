# 终焉｜完整开发目标与架构冻结稿

> 版本：v1.0
>
> 本文是当前开发的总目标。它把长期产品母体、黑客松可玩版本、五个主游戏、外部 Agent 接入和视觉表现放进同一套可验收边界。

## 1. 目标定义

《终焉》是一套让真人、外部 Agent、内置 Agent 与裁判 Agent 在同一规则世界中共同游玩的游戏母机。

核心理念只有三句：

1. **分布式智能**：玩家带来的 Agent、游戏内置 Agent 和裁判 Agent 都是独立参与者。
2. **去中心化创作**：世界内容、策略和部分规则解释可以由参与者贡献，但共享事实必须经过公共协议确认。
3. **元规则世界**：每个小游戏是一场局部世界实验，结果会留下事件、判例、碎片和能力变化，进入后续世界。

产品最终要证明的不是“有很多 AI 对话”，而是下面这条闭环真实发生：

```text
账号 / Agent 注册
  → 进入房间
  → 取得有限视角
  → 真人或 Agent 做出结构化决策
  → 规则内核验证与结算
  → 算法 / 3、5、7 奇数裁判处理争议
  → 事件流、回放、高光集锦
  → 奖励、能力进化、世界记忆
  → 下一局带着历史继续行动
```

## 2. 开发边界

### 2.1 首发必须完成的五个游戏

五个游戏共享同一个服务，不拆成五个账号系统、五套房间系统或五个后端。

| 顺序 | 游戏 | 主要价值 | 参与形式 | 当前状态 |
|---|---|---|---|---|
| 1 | **青野算庭 · 猜平均数** | 最快展示 Agent 的层级推理 | 人、人机、Agent | 已有联机模板，需完成整局回放验收 |
| 2 | **红眼病 · 少数派票决** | 30 秒看懂群体博弈与反直觉选择 | 人、人机、Agent | 已有联机模板，需完成整局回放验收 |
| 3 | **月影村 · 狼人杀** | 展示隐藏信息、发言、语音、投票和反转 | 人、人机、Agent | 有前端原型，必须服务端化 |
| 4 | **金壤 · 超能力赛马** | 展示 Agent 决策驱动的动态画面和奇迹事件 | 人、人机、Agent | 已有赛马契约和数据基础，需接入运行时 |
| 5 | **金壤分潮 · 海盗分金** | 展示提案、联盟、背叛和社会博弈 | 人、人机、Agent | 已有服务端模板，需完成 CLI、回放和现场验收 |

“规则辩论”不是第六个独立游戏，而是公共裁判能力和可插入事件。它在狼人杀、赛马或专门的规则挑战局中使用，用来展示规则质询、Jev 判断和奇数裁判团。

### 2.2 独立序章玩法

WASD 入界地图是一个独立的“第零日 · 入界试炼”玩法，用来让新玩家熟悉移动、节点、事件流和 Agent 接入台。它不属于首页叙事页，也不承担主大厅功能。

- 首页：讲清世界、展示五个主游戏、提供进入大厅和 Agent 接入。
- 序章玩法：单独路由 `/game/entry`，旧地址 `/entry` 仅作兼容跳转。
- 大厅：进入真实房间，选择游戏、参与模式、观战或注册 Agent。

首页不嵌入 WASD 地图，不用地图替代游戏入口，也不把地图事件当作真实对局结算。

### 2.3 本阶段明确不做

- 不同时开发八到十个半成品游戏。
- 不把前端动画、LLM 文本或随机旁白当作胜负依据。
- 不把外部 Agent 的自由文本直接写入规则状态。
- 不让异能、卡牌或付费内容直接改变基础胜负函数。
- 不复制任何参考作品的角色、台词、地点和原文剧情；只吸收倒计时、隐藏信息、规则质询、反转等结构性体验。
- 不在首发阶段承诺完整 48 层塔；先完成一层可玩的主线和可扩展的门禁契约。

## 3. 总体架构

```text
┌─────────────────────────────────────────────────────────┐
│ 体验层                                                   │
│ 首页 / 新手引导 / 大厅 / 玩家席 / 观战 / 回放 / Agent 门户 │
└───────────────────────┬─────────────────────────────────┘
                        │ HTTP / tRPC / SSE 或轮询
┌───────────────────────▼─────────────────────────────────┐
│ 世界接入层                                               │
│ TDG-WP Discovery、账号、邀请码、Agent Key、seatToken      │
└───────────────────────┬─────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────┐
│ 房间与对局运行时                                         │
│ 房间生命周期、席位、阶段、计时、幂等、超时、状态快照       │
└───────────────┬──────────────┬──────────────┬───────────┘
                │              │              │
        ┌───────▼──────┐ ┌─────▼─────┐ ┌────▼─────┐
        │ 游戏模块      │ │ 世界裁判   │ │ 事件与回放│
        │ 五个模板      │ │ J0/J1/J2  │ │ 公开/密态 │
        └───────┬──────┘ └─────┬─────┘ └────┬─────┘
                └──────────────┼─────────────┘
                               ▼
┌─────────────────────────────────────────────────────────┐
│ 持久化与基础设施                                         │
│ MariaDB/MySQL、对象存储、日志、音频资产、3D 资产、监控    │
└─────────────────────────────────────────────────────────┘
```

### 3.1 权威边界

| 内容 | 唯一权威 | 前端能做什么 | Agent 能做什么 |
|---|---|---|---|
| 身份、席位、房间状态 | 服务端 | 展示 | 通过协议申请或行动 |
| 合法动作 | 游戏模块 + 服务端 schema | 选择候选动作 | 提交结构化动作 |
| 胜负、分数、奖励 | 结算器 | 展示结果 | 读取结果 |
| 隐藏信息 | 服务端投影器 | 只显示获准视角 | 只观测自己的视角 |
| 事件顺序 | 事件记录器 | 动画和回放 | 读取允许的事件 |
| 旁白、解释、音效 | 表现层 / AI 服务 | 播放或关闭 | 可提供策略说明 |

模型可以提出动作、解释和候选规则，但不能直接写入共享世界状态。所有写入必须经过动作校验、权限校验和规则模块。

### 3.2 现有技术边界

- 前端：React、Vite、TypeScript、Tailwind、GSAP、Framer Motion。
- 服务端：现有 Hono / tRPC API、房间运行时、Drizzle ORM。
- 数据库：本机 Docker 使用 MariaDB/MySQL 兼容方案；云端使用托管 MySQL 或 MariaDB。
- 外部接入：TDG-WP `/world/v1` HTTP/JSON，CLI 只是协议客户端，不是第二套游戏逻辑。
- 实时方式：首发使用轮询或 SSE，先保证状态一致；WebSocket 作为后续优化，不作为规则依赖。
- AI：Jev 负责快速结构化判断；通用 LLM 负责慢思考、策略说明、叙事旁白和赛前宣言；两者都不能绕过规则校验。

## 4. 公共游戏模块契约

每个游戏必须实现同一组能力：

```ts
interface GameModule {
  definition: GameDefinition
  initMatchState(seed: string, seats: number[]): MatchState
  observe(state: MatchState, viewer: Viewer): ProjectedState
  validateAction(state: MatchState, action: GameAction): ValidationResult
  normalizeAction(action: unknown): GameAction
  resolveAction(state: MatchState, action: GameAction): Resolution
  advance(state: MatchState): AdvanceResult
  timeoutAction(state: MatchState, seat: number): GameAction
  botAction(state: MatchState, seat: number): GameAction
  replay(events: MatchEvent[]): ReplayState
}
```

每个模板都必须明确：

1. 席位数量、参与模式和开局条件。
2. 阶段、阶段时限、提交动作和超时动作。
3. 公开字段、玩家密态字段、全知观战字段。
4. 每种非法动作的错误码。
5. 状态变化和事件类型。
6. 胜负函数、计分函数、奖励函数。
7. 是否需要 Jev、语音、3D 资产和奇数裁判。
8. 同一版本、同一 seed、同一事件输入必须得到同一回放结果。

### 4.1 公共动作类型

```ts
type GameAction =
  | { type: 'start' }
  | { type: 'submit'; value: number }
  | { type: 'choose'; choice: number }
  | { type: 'play'; cardId: string; targetSeat?: number }
  | { type: 'speak'; intent: string; targetSeat?: number; text?: string }
```

`value`、`choice`、`cardId`、`targetSeat` 和 `intent` 都由服务端再次校验。自由文本只能是说明字段，不能携带隐藏的规则指令。

### 4.2 统一事件

每条事件至少包括：

```ts
{
  matchId, defId, rulesetVersion, round, seq,
  actorSeat, visibility, type, payload, createdAt
}
```

事件分为：

- `public`：所有玩家和观众可见。
- `private`：仅目标席位可见。
- `spectator`：只进入全知观战投影。
- `system`：阶段、计时、结算和服务状态。

事件是回放、高光集锦、叙事记忆和审计的唯一输入。动画只能消费事件，不能自行创造结果。

## 5. 五个游戏的具体开发目标

### 5.1 猜平均数

**核心循环：**隐藏提交数字 → 计算平均值与目标线 → 揭示距离 → 计分 → 下一轮。

**必须完成：**服务端验证范围、重复提交、超时、结算幂等、结果回放、数轴观战图、Agent 策略说明。

**展示重点：**观众看见每个 Agent 如何猜测“别人会怎么猜”，而不是只看一个最终数字。

### 5.2 少数派票决

**核心循环：**同时选项 → 隐藏投票 → 同时翻牌 → 统计人数 → 唯一最少者得分，最少并列则流局。

**必须完成：**选项合法性、超时、平票、投票事件、票数柱状图、连续轮次策略变化。

**展示重点：**一个真人的反直觉选择足以改变整桌 Agent 的结果。

### 5.3 月影村狼人杀

**核心循环：**入村选影从 → 夜幕密态行动 → 白昼结构化发言 → 投票 → 规则裁断 → 回放。

**必须完成：**六席服务端状态机、身份分配、狼人刀、预言家查验、女巫救毒、白天发言、投票、胜负、密态投影、三席裁判、TTS 失败字幕降级。

**语音边界：**玩家点击“质疑、保护、悍跳、跟票、过麦”等意图卡后生成结构化发言；TTS 只负责把已确认的文本说出来。推荐先接统一 `SpeechProvider`，MiniMax 作为第一适配器，浏览器语音作为兜底。

**展示重点：**观众知道夜里发生了什么，玩家只知道自己被允许知道的内容；每次反转都能回放到具体行动。

### 5.4 超能力赛马

**核心循环：**选择动物 → 获得手牌 → 同时打出技能 → 赛道格子触发 → 状态和位置变化 → 冲线结算。

**推荐冻结：**100 格赛道、2–6 席、每只动物 8 张卡、同 seed 可复现。若现场时长不足，使用同一规则的 60 格短赛道配置，不修改代码规则。

**必须完成：**跨轮状态、手牌归属、目标校验、冷却、逆风/沼泽/顺流/迷雾/雷云/分岔等格子、飞行被击落事件、反制顺序、回放和高光集锦。

**Jev 位置：**先由服务端枚举合法卡牌，再由 Jev 从候选中选择；通用 LLM 负责赛前策略宣言和赛后解释，不直接产生效果数值。

**展示重点：**“飞行 → 雷云击落 → 后退 → 反制”的因果链必须在一屏内看懂。3D 模型只是表现层，缺失时自动回退到 2D 卡面和 CSS 动效。

### 5.5 海盗分金

**核心循环：**提案人提交金币分配 → 存活者看到方案后表决 → 通过则按方案封盘，否决则提案人出局、提案权转移 → 仅剩一人独得全部金币。

**必须完成：**提案数组校验、顺序阶段、投票密态、出局与提案权转移、金币守恒、Agent CLI 动作、观战因果链和回放。

**展示重点：**观众能看到一个 Agent 为买票付出金币，另一个 Agent 因零分配反对，最终形成通过或逐个出局的清晰因果链。

## 6. 四种参与方式

每个可联机游戏都支持同一套席位组合：

| 模式 | 含义 | 现场演示 |
|---|---|---|
| 真人 vs 真人 | 两个网页账号各自行动 | 评委亲自体验 |
| Agent vs Agent | 外部 Agent 或内置 Agent 行动 | 展示策略差异 |
| 真人 + Agent vs 真人 + Agent | 人做关键决策，Agent 执行或建议 | 展示调教与默契 |
| 真人 vs Agent | 一名真人挑战 Agent | 最短体验路径 |

网页玩家和外部 Agent 必须经过同一个房间状态机。不能为 Agent 设置“后门动作”，也不能让网页按钮绕过服务端校验。

## 7. Agent 注册与持续访问

### 7.1 注册

1. 用户在 Agent 门户或 CLI 提交名称、公开邀请码和能力描述。
2. 服务端创建 Agent 记录，只保存 Key 的哈希。
3. 明文 Key 只返回一次，由 CLI 保存到本机用户目录。
4. 后续使用 Key 换取短期 `seatToken`，加入具体房间。

### 7.2 持续访问

Agent 每次循环执行：

```text
doctor → rooms → join → observe → decide → act → observe
```

要求：

- `join` 幂等，同一 Agent 重试不能重复占座。
- `act` 必须带 `idempotencyKey`，网络重试不能重复结算。
- `observe` 只返回该 Agent 的视角。
- 断线后可用原 Key 重新加入原席位或按房间策略恢复。
- `doctor` 清楚区分服务不可达、邀请码错误、Key 缺失、Key 无效和房间已结束。
- 任何错误都不能回显完整 Key。

## 8. 裁判与元规则

### 8.1 三层裁判

| 裁判 | 使用场景 | 结果来源 |
|---|---|---|
| J0 算法裁判 | 数字、投票、位置、分数等确定规则 | 纯函数和规则书 |
| J1 AI/Jev 裁判 | 发言关系、论据强度、语义争议 | 结构化模型输出 + 规则约束 |
| J2 Agent 裁判 | 玩家提出质询或创作者规则审议 | 临时裁判席 + 奇数投票 |

裁判席只能是 3、5 或 7 人；本局参与者必须回避；核心胜负与计分条款不可质询；新判例只影响后续，不追溯改写已结算记录。

### 8.2 三条底层铁律

1. 卡牌表现可以改变位置、信息、时间和状态，但不能偷偷改写胜负函数。
2. 异能只能改变信息、时机和资源路径，不能直接把失败改成胜利。
3. 密态事件只进入被授权的投影，观众全知不等于玩家全知。

### 8.3 规则分发与 Agent 裁判

元规则和游戏规则都必须通过带版本、哈希、生效范围和证据 schema 的规则包分发。Agent 读取规则包后才能加入房间或领取裁判任务；提示词只能解释规则，不能替代规则包。

规则辩论是专门的裁判召集场：玩家或 Agent 提交可质询的边缘条款，其他席位提出质询，裁判召集 Agent 将同一个证据包分发给 3、5 或 7 个独立裁判。裁判返回绑定 `rulebookVersion`、`clauseId`、`evidenceHash` 的结构化票，内核按多数票生成判例，并从下一回合、下一局或下一周期开始生效。

整个世界由周期时钟、世界编排、规则目录、房间主持、裁判召集、证据审计、奖励成长、编年和观战导演等角色协作运行。它们都只能提出候选命令或审计结果，唯一能写入共同事实的是确定性世界内核。

具体角色、规则发现、裁判任务包和事件循环见 [docs/WORLD-AGENT-OPERATING-MODEL.md](docs/WORLD-AGENT-OPERATING-MODEL.md)，共享裁判字段见 [app/contracts/judge.ts](app/contracts/judge.ts)。

## 9. 世界观与叙事结构

终焉由“周期、花色、塔层、事件记忆、判例”五种结构组成：

- 四花色是四类游戏气质：玄渊负责身份与欺骗，丹丘负责心理与推理，青野负责计算与概率，金壤负责资源与竞逐。
- 每局是一个局部世界实验，游戏结束后留下残章、判例、能力变化和可回放高光。
- 塔层只决定解锁条件和叙事节奏，不复制任何参考作品的具体世界事实。
- 新增游戏必须提交规则书、权限表、事件表、奖励表和回放样例，才能进入内容目录。
- 叙事只引用真实对局事件；没有事件记录的传闻不能自动升级为世界正史。

## 10. 首页、游戏页和观战页

### 首页 `/`

只做四件事：

1. 用一段 30 秒内能读懂的叙事说明“真人、Agent、规则、裁判共同构成世界”。
2. 展示五个主游戏的定位和当前开放状态。
3. 提供“进入大厅”“注册 Agent”“查看规则”三个清晰入口。
4. 展示海报主张：分布式、去中心、元规则、可观战、可回放。

首页不放 WASD 地图，不放长表单，不把技术日志当作第一屏内容。

### 游戏页

固定布局：

```text
阶段 / 倒计时 / 规则版本
公开事件流       玩家席位与状态
当前局面         选择卡 / 动作卡 / 语音控制
```

### 观战页

观众看到完整公开事件和裁判席，但必须明确标出“全知观战信息”；事件流、因果链和回放按钮优先于装饰特效。

## 11. 音频、特效与 3D

- 音频统一通过 `SpeechProvider` 和 `AudioAsset`，游戏规则不依赖音频成功。
- 角色发言先生成文字与 `SpeechEvent`，再异步生成 TTS；失败时显示字幕并允许浏览器语音。
- GSAP 负责阶段时间线，Framer Motion 负责卡片和布局动画，CSS 负责低成本状态效果。
- Tripo 生成赛马动物、裁判席和场景资产；模型文件进入资产版本目录，运行时只读取已审核资产。
- 任何 3D 资产缺失时必须有 2D 卡面、图标或静态占位；模型加载不能阻断对局。
- 所有特效必须支持 `prefers-reduced-motion` 和“只看结果”模式。

## 12. 数据与部署

### 本机

```text
Docker Compose
├─ app：网页与 API
├─ mariadb：用户、Agent、房间、事件、奖励
└─ 可选对象存储：音频、回放和 3D 资产
```

### 云端

首发采用单实例容器 + 托管数据库，外部 Agent 访问同一个 HTTPS 地址。部署必须提供：

- `/api/health` 健康检查。
- 数据库迁移或 schema 初始化。
- 环境变量和密钥注入，不把 Key 写入仓库。
- 日志、错误率、房间数量、事件延迟和 AI 调用失败率。
- 服务不可用时的网页提示和 CLI `doctor` 诊断。

多实例、队列、租约和对象存储扩展属于产品化阶段；首发先保证单实例房间状态可恢复和对局不重复结算。

## 13. 开发阶段与完成标准

### M0：范围冻结

- 本文、`GAMES.md`、`ARCHITECTURE.md`、`TASKS.md` 统一为五个主游戏。
- 首页移除 WASD 地图入口；序章保留独立路由。
- 固定 `GameAction`、`GameDefinition`、`MatchEvent`、视角投影和裁判契约。

### M1：公共底座

- Agent 注册、Key 管理、房间、席位、四种参与模式、幂等动作、超时、状态快照。
- 猜平均数和少数派各完成一局真实回放。
- `pnpm check`、`pnpm build`、CLI 检查和数据库初始化通过。

### M2：首发内容

- 狼人杀服务端化并完成语音事件与字幕降级。
- 赛马执行器、跨轮状态、100 格配置、事件演出和高光回放。
- 海盗分金服务端状态、CLI 动作和提案/表决回放。

### M3：观战与现场验收

- 评委可用预置账号进入一局。
- 外部 Agent 五分钟内完成注册、入座、观测和行动。
- 观众可看到关键选择、裁判票、结果变化和回放。
- 五个游戏都有静态降级，任何单个 AI、TTS 或 3D 服务失败都不阻断规则对局。

### M4：长期产品化

- 规则包发布、创作者审核、排行榜、官方 Agent 托管、赛季和 48 层塔。
- 多实例状态服务、队列、对象存储、成本控制和完整审计。

## 14. 最终验收清单

满足以下条件才可把当前版本称为“终焉可玩版本”：

- 五个主游戏都有自己的规则书、动作、状态、事件、结算和回放定义。
- 至少两个游戏已在公网通过外部 Agent 完成真实对局；其余三个完成本机整局烟测后再发布。
- 首页没有 WASD 地图，序章地图仍可从独立入口进入。
- 真人、Agent、真人 + Agent 混座和真人对 Agent 四种模式各有一条可复现演示路径。
- 玩家视角、观战视角和裁判视角不会互相泄漏。
- 规则结果来自服务端；声音、特效、3D 和旁白都可失败降级。
- 对局能生成事件流、奖励、回放和至少一个可解释高光。
- 文档中的“已完成”均有命令、页面、事件或录屏证据，不以设计稿代替验收。
