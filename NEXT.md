下一批需求：技术判断与设计

本文档回答你最新提的八件事。凡是我查过代码得出的结论都标了依据，
凡是需要你决策的都明确标出——不替你拍板。

---

## 一、双身份狼人杀（六人局）

### 为什么「双身份」是个好设计

单身份狼人杀的问题是**信息一次性给定**：开局知道自己是谁，
之后只是推理别人。双身份把身份本身变成变量，推理对象多了一层。

建议的机制（六人局）：

| 座位 | 明身份（开局公开） | 暗身份（秘密） |
|---|---|---|
| 6 席 | 村民 / 医者 / 守夜 / 账房 / 说书 / 匠人 | 狼 ×2、预言 ×1、女巫 ×1、民 ×2 |

**明身份**决定你能做什么动作（账房能查碎片流向、说书能公开一条残章），
**暗身份**决定你的阵营。两者独立随机组合。

这样产生的新博弈：「他是账房」是公开事实，但账房是狼还是好人未知。
玩家要同时推理「他的暗身份」和「他为什么这样用明身份的能力」。

### 关键取舍：明身份能力不能改胜负

这条必须守住，否则每加一个明身份都要重新平衡全局。
明身份只给**信息类能力**（查看、公开、标记），不给刀/救/毒。
这与 `contracts/ability.ts` 的铁律同源——异能只改信息与时间。

### 隐藏任务系统

你说「每把任务不一样，完成得碎片」。建议这样做：

```
任务池按明身份分组，每把每人抽一个：
  账房：本局结束时碎片余额为全场最高       → 得 ♦ 碎片 ×15
  说书：让至少两人引用你公开的残章         → 得 ♠ 碎片 ×15
  守夜：准确指出首夜被刀者                 → 得 ♠ 碎片 ×20
  医者：本局无人因你的判断而误死            → 得 ♥ 碎片 ×15
```

**任务对他人不可见**，这是它有趣的地方：你看到某人行为古怪，
不知道他是狼还是在做任务。**这一条直接增加了推理噪声**，
让"行为异常"不再等于"是狼"。

实现上任务是纯事件流判定——和 `api/world/eggs.ts` 的彩蛋判定同一套思路
（吃 `MatchEvent[]` 吐结果），可以复用那个模式，零新算法。

**前提**：依赖事件流落库（P0 那项）。任务判定读的就是 `match_logs`。

### 对话与选项

「通过对话和选项完成任务」——建议用**结构化选项而非自由文本**：

```
你的回合，选一句发言：
  1. 我是账房，昨夜查到 3 号有异常流水。   （真话，暴露明身份能力）
  2. 我什么也没查到。                      （假话，保留信息）
  3. 我怀疑 5 号，但说不出理由。            （模糊，不承诺）
```

理由是三条：AI 能可靠解析结构化选项；语音识别的错误率不影响机制；
选项本身就是设计好的博弈空间（每个选项都有代价）。

自由文本可以作为**补充**（打字框），但机制判定只认选项。

### 语音对话

这是个明确的取舍，我建议**先不做语音识别，只做语音播放**：

| 方向 | 成本 | 建议 |
|---|---|---|
| 文字 → 语音（TTS，AI 发言朗读） | 低 | ✅ 做，氛围提升大 |
| 语音 → 文字（ASR，玩家说话） | 高 | ❌ 72 小时内不做 |

理由：AI 发言朗读能立刻提升沉浸感（八影从各配一个音色），
而玩家语音输入涉及降噪、断句、超时处理，出错会直接卡住对局。
选项式发言点一下就完事，比说话还快。

若一定要做玩家语音，用浏览器原生 `SpeechRecognition` API（零依赖），
且**只用于填充自由文本框，不参与机制判定**。

---

## 二、六人并发：现有架构够用

我查了代码，这题的答案是**不需要改架构**，但有一个约束要知道。

### 为什么够用

`SdkRoom` 用 promise 链串行化所有状态变更（`runtime.ts:103,161`）：

```ts
private queue: Promise<unknown> = Promise.resolve();
private enqueue<T>(fn) { const run = this.queue.then(fn); ... }
```

这相当于一个 actor 邮箱——**六个人同时提交也会被排成一列处理**，
不存在竞态。这个设计已经解决了并发正确性。

流量上更不是问题：现有轮询 1.5 秒一次（`GuessOnline.tsx:69`），
6 人 = 4 请求/秒/房间。十个房间也才 40 req/s，Node 单进程轻松扛住。

### 真正的约束：单实例

房间存在内存 `Map<code, SdkRoom>`（`registry.ts:161`），
所以**多实例部署会导致房间不同步**。72 小时比赛单实例够用，
不要引入 Redis 增加复杂度。

赛后若要扩展，方案是把房间状态外置（Redis）或按房间码做一致性哈希路由。

### 若要提升实时性

轮询 1.5 秒对狼人杀的发言节奏够了。若觉得不够，**用 SSE 而非 WebSocket**：
Hono 原生支持，单向推送正好匹配「服务端状态变更 → 客户端刷新」的模式，
比 WebSocket 少一半复杂度。

---

## 三、影从接入用户 AI（CLI / Skill）

### 现状：HTTP 网关已完全可用

六个端点，任何语言能接（`api/agentRouter.ts`）：

```
register         → 得 tdg_ 前缀 API Key
gatewayRooms     → 查房间
gatewayJoin      → 入座得 seatToken
gatewayObserve   → 座位视角观测（密态已过滤）
gatewayAct       → 提交动作
gatewayRulebook  → 读规则书全文
gatewayAppeal    → 发起规则质询
```

### 三种接入形式的取舍

| 形式 | 成本 | 演示效果 | 建议 |
|---|---|---|---|
| **Skill 协议** | 极低（一个 md 文件） | 很好 | ✅ 优先 |
| HTTP 裸接 | 零（已可用） | 一般 | ✅ 已有 |
| npm CLI | 高（要发包） | 好 | ⚠️ 时间够再做 |

**Skill 优先的理由**：写一个 `SKILL.md` 描述六个端点与决策接口，
玩家在自己的 Claude Code / Codex 里说「帮我写个狼人杀 Agent 接入 ABCD 房」，
它就能自动生成并跑起来。**成本几乎为零，但现场让评委用自己的 AI 接入
比任何 PPT 都有说服力。**

建议的 Skill 文件结构（放 `skills/tdg-agent/SKILL.md`）：

```
---
name: tdg-agent
description: 接入《十日牌局》对局，用你的策略操作一个席位
---

## 接入流程
1. 用 TDG_API_KEY 环境变量认证
2. gatewayJoin 入座
3. 循环：gatewayObserve 观测 → 你的策略 → gatewayAct 提交
4. 可选：gatewayRulebook 读规则，gatewayAppeal 质询

## 端点详情
（六个端点的 curl 示例）

## 策略接口
你需要实现一个函数：view → action
```

---

## 四、终端里玩（不局限网页）

这件事**现在就已经技术可行**，因为 `gatewayObserve` 返回的是完整投影视图。
需要的只是一个 TUI 客户端。

### 建议形态

```bash
npx tdg play --room ABCD --key tdg_xxx
```

终端里渲染：座位环、当前阶段、倒计时、可选动作。
用 `blessed` 或 `ink`（React for CLI）都行，ink 更好写。

### 为什么这个值得做

它证明了一件事：**平台的 API 是完整的，网页只是其中一个客户端**。
这对「分布式智能平台」的定位是很强的支撑——
人可以在终端玩，AI 可以在终端跑，两者用同一套 API。

### 但要注意工作量

TUI 要处理终端尺寸、键盘事件、重绘节流。**一个人两天**。
建议排在观战页之后——观战页是演示必需，TUI 是加分项。

---

## 五、AI 驱动的回合制同时决策对战

你描述的「双方同时决策然后打架」——这正是超能力赛马已经用的结构
（秘密提交 → 同时揭晓 → 因果结算），但你想要的是更直接的战斗。

### 建议：镜心斗（♠ 玄渊）

两人（或 2v2）同时出招，招式相克，血量归零者负：

```
三类招式，石头剪刀布式相克：
  攻  破 守，被 闪 化
  守  破 闪，被 攻 破
  闪  破 攻，被 守 破

但每招有「势」的层次（1–3 级），同类相遇则高势者胜。
势由前一回合的结果累积：赢了 +1，输了归 0。
```

**为什么这个设计适合 AI 对抗**：它是有记忆的重复博弈。
AI 要推理「对手会不会认为我要出攻」，而势的累积让纳什均衡随局势移动。
观众看到的是**招式碰撞的演出 + 势的可视化增长**。

Jev 在这里做什么：一次调用三问——
`move`（choice，从三招中择一）、`level`（choice，用几级势）、
`read`（score，对手下一招的可预测性 0–2）。约 300ms，符合回合节奏。

### 与赛马的区别

赛马是**多方竞速**（六方，看位置变化），镜心斗是**双方对决**（看血量与势）。
两者的观赏点不同，不重复。赛马适合展示混战，镜心斗适合展示深度。

---

## 六、还有什么游戏能增加氛围感

按「氛围贡献 ÷ 开发成本」排，推荐两个：

### 夜话（♥ 丹丘）· 非竞技的氛围游戏

不是博弈，是**叙事**。玩家轮流讲述自己这十日的经历（从选项中选），
其他人（含 AI）投票选出「最可信的谎」。

价值：它给紧张的博弈之间一个喘息，且**产出残章**——
玩家讲的故事片段会成为世界观的一部分（存 `rulings` 类似的表）。
这让「世界观构筑平台」这个定位真正落地。

成本极低（就是选项 + 投票），氛围贡献很大。

### 残章拍卖（♦ 金壤）

全支付拍卖：所有人出价竞争一张残章卡，**出价一律扣除，只有最高者得卡**。

价值：一轮定胜负，30 秒一局，适合作为**开场暖场**。
且它自然地解释了碎片经济——你会真切感到碎片是稀缺的。

---

## 七、影从作为人物模板

现有八影从已有人设、花色、生肖、异能（`src/data/echoes.ts` + `abilities.data.ts`）。
你说「影从是不同人物模板」，我理解是要让它们**可被用户 AI 扮演**。

建议的结构：

```ts
// 影从模板 = 人设 + 决策倾向 + 发言风格
interface EchoTemplate {
  echoId: string;
  // 给用户 AI 的系统提示词（让它扮演这个影从）
  personaPrompt: string;
  // 决策参数（现有 agents.ts 已有）
  params: { aggression, deception, levelK, noise };
  // 发言模板池（现有 speech.ts 已有）
  speechStyle: SpeechStyle;
}
```

用户接入时选一个影从模板，**他的 AI 就以那个人格出战**。
这让「影从」从 NPC 变成了**用户 AI 的皮肤与人格约束**——
同一个用户换影从，打法就不同，因为 personaPrompt 变了。

这是个很好的设计：它让世界观与机制真正绑定。

---

## 八、关于用 GPT 参与开发

你说为节约 token 必须调 GPT。我得说清一件事：

**我无法直接调用 GPT**——我没有那个工具。但你机器上装了 `codex` CLI
（我验证过，`codex exec` 可非交互调用），它背后就是 OpenAI 的模型。

所以实际可行的分工是：

| 工具 | 谁来调 | 负责什么 |
|---|---|---|
| **Fable 5**（我） | 你直接对话 | 架构、契约层、跨文件重构、代码评审 |
| **Codex CLI** | 你在终端跑，或我用 Bash 调 | 单文件实现、批量数据生成 |
| **Kimi K3** | 你直接用 | UI 设计方案 |
| **Jev** | 代码里调 | 运行时判断（已接入） |

如果你希望我把某个模块派给 Codex，直接说「把 X 派给 codex」，
我会构造好 prompt（含硬约束）然后用 Bash 调用它。
派活模板已写在 `TEAMWORK.md` 第九节。

**适合派给 Codex 的活**（单文件、规则清晰）：
- 异能数据 8 → 48 条
- 48 层 `FloorDef` 数据
- 任务池数据（每个明身份 4–5 个任务）
- 单个页面的视觉优化

**不适合派出去的**：契约层改动（容易破铁律）、跨文件重构。

---

## 九、还需要做什么（完整清单）

按依赖排序，标了预估工作量：

### 必须做（演示成败点）

| 项 | 量 | 依赖 |
|---|---|---|
| 事件流落库（`runtime.ts` 埋点） | 半天 | — |
| 观战页 + 裁判席 UI | 一天 | 事件流 |
| 推送到 GitHub + 三人认领分工 | 1 小时 | — |
| 字体自托管（防现场断网） | 30 分钟 | — |

### 高价值

| 项 | 量 | 说明 |
|---|---|---|
| 双身份狼人杀 + 任务系统 | 两天 | 依赖事件流 |
| 狼人杀联机化 | 两天 | 最有观赏性 |
| 超能力赛马 | 两天 | 契约已就绪 |
| Agent Skill 文件 | 半天 | 成本极低，效果好 |
| TTS 发言朗读 | 半天 | 氛围提升大 |

### 加分项

| 项 | 量 |
|---|---|
| 达芬奇密码 + 概率热力图 | 一天半 |
| 镜心斗（同时决策对战） | 一天半 |
| 终端 TUI 客户端 | 两天 |
| 48 层爬塔数据 + 门禁接线 | 一天 |
| 残章拍卖 / 夜话 | 各半天 |

### 别做

- 玩家语音识别（ASR）：出错会卡住对局
- 阿瓦隆：与狼人杀机制重叠
- 多实例部署 / Redis：72 小时内是负担
- WebSocket：SSE 就够，少一半复杂度
