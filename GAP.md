离最终目标还缺什么

本文档每一条都经过代码核实，标了核实方式。
结论先说：**你的架构完成度很高，但有一条主动脉没接上**——
因为它没接上，六个已经写好的系统全都悬空。

---

## 一、最关键的发现：一条主动脉断了

### 事件流没有生产端

核实方式：`grep -rln "matchStart|insertMatchLog" api src`
结果：只有三个文件命中——`queries/matchLogs.ts`（我写的落库层）、
`world/eggs.ts`（消费者）、`eggs.test.ts`（测试手造数据）。

**没有任何代码产出 `MatchEvent`。** 对局在跑，但不留痕。

### 为什么这一条卡住了六个系统

这不是「还差一个功能」，而是**六个已完成的系统共同依赖的那一个环节缺失**：

| 已写好的系统 | 代码量 | 因此而悬空 |
|---|---|---|
| 彩蛋判定器 | 269 行 + 33 项测试 | 无事件流可吃，**从未被调用** |
| 事件流投影 | `projectEvents` | 无事件可投影 |
| 观战 / 回放 | 后端已就绪 | 无数据源 |
| 情报卡 | 契约已定义 | 靠统计事件流生成，无从统计 |
| 隐藏任务（你要的） | 未写 | 判定方式同彩蛋，也依赖它 |
| 裁判团的真实种子 | 已实现 | 暂用「房间码+开局时刻」凑 |

核实：`grep -rn "grantCard|detectEggs" api` — `detectEggs` 零调用方，
`grantCard` 只在质询路径被调，**常规对局结算不发任何卡牌**。

也就是说：现在赢一局只加碎片（`awardGameResult`），
**不掉残章、不触发彩蛋、不留对局记录**。

### 这条链要做什么

在 `api/games/sdk/runtime.ts` 的现有节点埋点，事件类型都已定义好：

```
入座完成 → matchStart      发身份 → secretAssign（打 secret 标记）
每轮开始 → roundBegin      玩家交 → action
揭晓     → reveal          淘汰   → eliminate
结算     → matchEnd  然后 insertMatchLog + detectEggs + grantCard
```

**半天工作量，解锁六个系统。** 这是整个项目投入产出比最高的一件事。

---

## 二、其他断开的闭环（都已核实）

### 异能系统：写好了，没接线

核实：`grep -rn "canUseAbility|abilityOfEcho" api src`
结果：**只有测试文件引用**。

- `contracts/ability.ts` 178 行，五个钩子、触发条件、充能规则齐备
- `abilities.data.ts` 八影从各一个异能
- 但 `runtime.ts` 里没有任何钩子调用点

缺的是：在对局管道的五处插入钩子调用（`onProject` / `onBeforeDecide` /
`onSubmit` / `onReveal` / `onSettle`），以及一个前端的异能按钮。

**约一天。** 做完「契约选影从 = 选异能」这句话才成立。

### 残章拼图：卡能掉，但组拼不出来

核实：`grep -rn "RELIC_SETS|completedRelicSetIds" api src`（排除 contracts）
结果：**只有测试引用**。

`contracts/relics.ts` 488 行数据、`RelicSet` 定义、`checkGate` 判定都在，
但没有任何代码判断「玩家是否集齐了某一组」。

缺的是一个查询：读 `player_cards` → 比对 `RELIC_SETS` → 算出已完成的组。
`api/queries/playerCards.ts` 的 `ownedCardIds` 已经为此准备好了。

**约两小时。** 做完塔门门禁与残章解锁才能用。

### 天梯：显示的是假数据

核实：`src/pages/Leaderboard.tsx:14-15` 引用的是
`@/components/content/leaderboard-data` 的 `buildBoard`——本地构造，
**不读任何 tRPC 接口**。

「身份盲盒」（不标注谁是 AI 的榜单）这个彩蛋设计很好，
但现在榜上没有真人。

**约半天。** 需要一个按积分排序的查询 + 前端换数据源。

### 爬塔：两套并存，不相通

核实：`FloorDef` / `TOTAL_FLOORS` / `checkGate` 在 `src` 下零引用。

- 契约层的塔（48 层谜题链，门禁递进）：设计完整，**数据文件不存在**
- 实际在跑的塔（`src/data/spire`）：可玩，**不读契约层门禁**

契约层那套设计里最妙的一笔是第 25 层的 `anyRuling` 门——
强制玩家质询成功一次才能上去，**让裁判机制从可选玩法变成主线必经**。
现在这个设计一行数据都没有。

**约一天**（48 层数据约 200 行 + 门禁接线）。

---

## 三、还能做哪些游戏

按「观赏性 ÷ 成本」排。前四个我认为值得做，后面的按需。

### 已设计完、可直接实现

| 游戏 | 花色 | 状态 | 量 |
|---|---|---|---|
| 超能力赛马 | — | 契约 + 48 卡 + 赛道已就绪，37 项测试 | 两天 |
| 海盗分金 | ♦ | 规则书已写，缺模板实现 | 一天 |

### 推荐新增

**达芬奇密码（♥）· 一天半**

信息结构极干净：自己看不见自己的牌，别人能看见，信息完全离散无歧义。
它是**纯贝叶斯推理**——AI 能算出「已知翻开牌的条件下，对手第 3 张是 7 的概率」。

杀手级演出：**概率热力图**。观战时显示每个 AI 对每张未知牌的概率估计，
观众能直接看到 AI 脑中的分布逐轮收缩。桌游原版做不到这个。

**镜心斗（♠）· 一天半**

双方同时出招，三招相克（攻破守、守破闪、闪破攻），
但每招有「势」1–3 级，同类相遇高势者胜。势由前轮结果累积：赢 +1，输归 0。

为什么适合 AI：**有记忆的重复博弈**。纳什均衡随势的累积而移动，
AI 要推理「对手是否认为我要出攻」。观众看招式碰撞 + 势的增长条。

与赛马不重复：赛马看多方竞速，镜心斗看双方深度。

**千轮猜数（♣，agent-only）· 半天**

现有猜数改参数即可（轮数上限放到 1000，提交窗下限放到 1 秒）。
价值是**做人类做不到的事**：观众看 level-k 收敛曲线从散布收缩到均衡附近。

**建议至少做一个 agent-only 游戏**，否则「AI vs AI」只是把人换成 AI。

### 氛围类（成本极低）

**残章拍卖（♦）· 半天**
全支付拍卖：所有人出价竞拍一张残章，**出价一律扣除，只最高者得卡**。
30 秒一局，适合开场暖场，且让玩家真切感到碎片稀缺。

**夜话（♥）· 半天**
不是博弈是叙事。轮流讲述这十日的经历（选项式），众人投票选「最可信的谎」。
它产出残章——玩家讲的片段成为世界观的一部分。
**这让「世界观构筑平台」的定位真正落地。**

### 不建议做

| 游戏 | 原因 |
|---|---|
| 阿瓦隆 | 与狼人杀机制重叠，未提供新观赏维度，而成本不低 |
| 海龟汤 | 需 AI 裁判做开放语义判断，Jev 的 choice 原语装不下 |
| 麻将 / 斗地主 | 规则复杂度高但博弈观赏性低，且与世界观不搭 |

---

## 四、你的最终目标 vs 现状

按你描述的目标逐项对照：

| 目标 | 现状 | 缺口 |
|---|---|---|
| 分布式智能架构 | ✅ 契约层 + Agent 网关完整 | — |
| 十日终焉世界观 | ✅ 四大陆/位阶/影从/残章齐备 | 裁判与判例的文案 |
| 四种对抗模式 | ✅ 一个 `SeatKind` 统一表达 | — |
| 多个小游戏 | ⚠️ 5 个可玩，2 个已设计 | 建议再加 2–3 个 |
| 奖励卡牌 | ⚠️ 契约+表+发卡函数齐备 | **没接进结算**（依赖事件流） |
| 异能进化 | ⚠️ 异能有，进化无 | 钩子未接线 + 无进化路径 |
| 分布式裁判 3/5/7 | ✅ 已实现，99 项测试 | 前端裁判席 UI |
| 规则演化（判例） | ✅ 后端完整 | 前端呈现 |
| 用户 AI 接入 | ✅ HTTP + Skill 已就绪 | 示例脚本 |
| 终端里玩 | ❌ | TUI 客户端（两天） |
| 语音 | ❌ | 建议只做 TTS |
| 双身份狼人杀 | ❌ | 依赖事件流 |
| 隐藏任务 | ❌ | 依赖事件流 |

**完成度约 65%**，但缺口高度集中——大部分卡在事件流这一条。

---

## 五、建议的执行顺序

前三步严格依赖，不能并行也不能跳：

### 第一优先（解锁一切）

1. **事件流落库**（半天）
   `runtime.ts` 埋点 → `insertMatchLog` → `detectEggs` → `grantCard`
   一做完，彩蛋/观战/回放/情报卡/任务系统五项同时解锁

2. **残章组判定**（两小时）
   读 `player_cards` 比对 `RELIC_SETS`，塔门与残章解锁才能用

3. **观战页 + 裁判席 UI**（一天）
   后端已就绪，这是演示的核心画面

### 第二优先（补闭环）

4. 异能钩子接线（一天）——让「选影从 = 选异能」成立
5. 狼人杀联机化（两天）——最有观赏性的游戏现在只能单机
6. 天梯接真数据（半天）——「身份盲盒」彩蛋才有意义

### 第三优先（加内容）

7. 超能力赛马（两天，契约已就绪）
8. 达芬奇密码 + 概率热力图（一天半）
9. 双身份狼人杀 + 隐藏任务（两天）
10. 48 层爬塔数据 + 门禁（一天）

### 演示前必做（别忘）

- **字体自托管**（30 分钟）——现在走 Google Fonts，断网则印章字失效
- **录屏备份**（一小时）——现场网络/数据库故障是常见事故
- **推送 GitHub**（需你执行）——`git push -u origin main`

---

## 六、一个判断

如果时间只够做三件事，我建议：

1. **事件流落库** —— 不做这个，你已写的 269 行彩蛋判定器
   和 488 行残章数据永远是死代码
2. **观战页 + 裁判席** —— 裁判机制是你最大的原创性，
   但它现在只存在于测试里，评委看不见
3. **超能力赛马** —— 唯一能在一个画面里同时展示
   多方博弈与因果链的玩法，契约已就绪

这三件做完，你有一个**能看、能演、机制完整**的作品。
其余都是加分项。
