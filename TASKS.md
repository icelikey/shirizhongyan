小游戏开发分工单

**把本文档直接发给组员。** 每张任务卡都是自包含的：
读完一张卡就能开工，不必再问你细节。

---

## 开工前：所有人必读

### 三条铁律（破了要重新平衡全局）

来自 `contracts/` 的三个文件，这是架构能横向扩展小游戏的原因：

1. **卡牌不参与胜负计算**（`cards.ts`）
   卡决定你能解开什么、进入哪里、引用什么条款，**不决定伤害数值**。
   所以新游戏上线不需要重新平衡卡池——规则内核从不读取卡牌。

2. **异能只改「信息」与「时间」**（`ability.ts`）
   加时、多看、看歪、改手、窥见推理链——都可以。
   改胜负、改计分——不行。
   所以 48 种异能 × N 个游戏只需要 5 个接入点。

3. **密态事件只出现在全知投影**（`matchLog.ts`）
   新增密态事件打个 `secret: true` 标记即自动受保护。
   座位投影必须剥掉它们，否则就是信息泄漏。

**提交前自问**：我的改动是否破了这三条？

### 环境搭建（10 分钟）

```bash
git clone https://github.com/icelikey/shirizhongyan.git
cd shirizhongyan/app
pnpm install
cp .env.example .env
```

`.env` 里 Kimi 两个 URL **必须填合法地址**（不走真实登录时不会被调用），
否则后端会因 `new URL("")` 崩掉，大厅全部报 500：

```env
KIMI_AUTH_URL=https://auth.kimi.com
KIMI_OPEN_URL=https://open.kimi.com
VITE_KIMI_AUTH_URL=https://auth.kimi.com
```

数据库要 **MySQL 8**，不是 MariaDB（schema 用了 `serial AUTO_INCREMENT`，
MariaDB 10.4 不认）。有 Docker 的话：

```bash
docker run -d --name tdg-mysql -p 3307:3306 \
  -e MYSQL_ALLOW_EMPTY_PASSWORD=yes \
  -e MYSQL_DATABASE=ten_days_gambit mysql:8
# .env 里 DATABASE_URL 端口写 3307
pnpm db:push
pnpm dev
```

### 验证你的改动

```bash
pnpm check                              # tsc -b，必须 0 错误才能推
npx vitest run contracts/ api/world/    # 应 136 项全绿
```

### 世界观速查

| 花色 | 大陆 | 语义 | 色值 |
|---|---|---|---|
| ♠ | 玄渊 | 博弈与欺骗 | `#8B93F8` |
| ♥ | 丹丘 | 心理与谈判 | `#EE6A72` |
| ♣ | 青野 | 计算与概率 | `#4ECB9C` |
| ♦ | 金壤 | 资源与竞逐 | `#F2A93B` |

位阶：黄 → 玄 → 地 → 天（出自《千字文》「天地玄黄」），天阶之上传说有「洪荒」。

**写文案的纪律**：中文，文言与白话混用，每段不超过三句。
机制先行——文案要能解释机制，不只是装饰。
反例「这是一个充满谎言的世界」（纯装饰）；
正例「明镜不言，只是照见」（解释了 AI 裁判为何不给理由）。

---

## 任务卡 · B1 · 海盗分金（♦ 金壤）

**难度** 中 · **预估** 一天 · **依赖** 无

### 为什么做这个

它是唯一能体现**联盟与背叛**的博弈。提案者要算清「给谁多少才能买到票」，
被提案者要判断「现在接受还是赌下一轮能拿更多」。AI 的算计肉眼可见。

### 规则书已写好

`contracts/rulebooks.data.ts` 里的 `RB_PIRATE`，七条条款，直接照着实现。
注意哪些是 `victory`（不可质询）、哪些是 `edge`（可质询）。

### 要做的

新建 `api/games/sdk/pirateGold.ts`，实现 `TemplateModule` 接口
（照 `pollDuel.ts` 的写法，111 行，是最短的参考）。

核心流程：
```
序号最小的存活者提案（分配全部金币，每份非负整数）
  → 全员表决
  → 赞成 ≥ 半数（含提案人）则通过，否则提案人出局，提案权下移
  → 仅剩一人时其方案自动通过
```

### 实现要点

- **用新的 `play` 动作**（`contracts/room.ts` 已扩展）承载提案，
  因为提案是一个数组而非单个数字，`submit` 装不下
- 需要 `initMatchState`（`templates.ts` 已加此可选方法）记录
  存活者、当前提案人、金币总数
- `resolveRound` 可返回 `finished: true` 提前终局
- 席位策略设为 `mixed-required`，最少 2 人 + 2 Agent
  （人的非理性正是看点）

### 注册

在 `api/games/sdk/registry.ts` 加官方定义常量 `PIRATE_CORE`，
并在 `moduleFor()` 里加路由分支。

### 完成标准

- 六人局能跑完（含提案人被否决出局的分支）
- `pnpm check` 0 错误
- 写一个 `pirateGold.test.ts`，至少覆盖：偶数人时的半数判定、
  仅剩一人的自动通过、超时视为反对

---

## 任务卡 · B2 · 达芬奇密码（♥ 丹丘）

**难度** 中 · **预估** 一天半 · **依赖** 无

### 为什么做这个

**它是全部候选游戏里最适合 AI 对抗的一个**，因为信息结构极干净：
自己看不见自己的牌，别人能看见，信息完全公开且离散，没有自然语言歧义。

它是**纯贝叶斯推理**——AI 能算出「已知翻开牌的条件下，对手第 3 张是 7 的概率」。

### 杀手级演出：概率热力图

这是本作独有、桌游原版做不到的：观战时显示每个 AI 对每张未知牌的概率估计，
**观众能直接看到 AI 脑中的分布逐轮收缩**。

这比看人皱眉思考强得多，也是这张卡真正的价值所在——
请把热力图当作必做项，不是加分项。

### 规则

- 牌堆：黑 0–11 + 白 0–11 共 24 张
- 每人发 4 张，**自己不可见，其余人可见**
- 手牌须按数字升序排列（同数则黑在白前），这是推理的关键约束
- 轮流猜「某人第 N 张是某数字」，猜中则该牌翻开，可继续猜或停手
- 猜错则自己最左侧未翻开的牌翻开
- 全部牌被翻开者淘汰，最后剩下者胜

### 要做的

1. `contracts/davinci.ts` — 牌、手牌、可见性契约
2. `api/games/sdk/davinci.ts` — `TemplateModule` 实现
3. 概率计算（纯函数，前后端共用）：
   给定已翻开的牌与升序约束，算出每个未知位置的数字概率分布
4. 前端热力图组件

### 实现要点

- **升序约束大幅收缩概率空间**，这是算法的核心：
  若某人第 2 张已知是 5，则第 1 张必 < 5、第 3 张必 > 5
- 概率算法要写成纯函数并单测，因为热力图的可信度全靠它
- 猜测用 `play` 动作：`{ cardId: "seat3-pos2", targetSeat: 3 }`
  配合一个数字载荷——或者你也可以扩展 payload，与 A 号商量

### 完成标准

- 四人局能跑完
- 概率函数有单测（至少 10 项，覆盖升序约束的推论）
- 热力图能随每轮翻牌更新
- `pnpm check` 0 错误

---

## 任务卡 · B3 · 千轮猜数（♣ 青野，agent-only）

**难度** 低 · **预估** 半天 · **依赖** 无

### 为什么做这个

**做人类做不到的事。** 观众能看到一条 level-k 收敛曲线——
从初始的随机散布逐渐收缩到纳什均衡附近。这是纯 AI 对抗独有的美感。

如果全部游戏都是「人的位置换成 AI」，那「分布式智能」的说服力不够。
**这张卡是那个反例。**

### 要做的

大部分是改参数，不是写新模板：

1. `contracts/gameSdk.ts` 的 `SDK_LIMITS` 把 `rounds.max` 从 10 放到 1000，
   `submitWindowSec.min` 从 10 放到 1
2. 加官方定义 `GUESS_MILLE_CORE`，`seatPolicy: "agent-only"`
3. **收敛曲线可视化**：这才是重点。用 recharts（已在依赖里）
   画出每轮各座位出数的散点 + 均值线

### 注意

`seatPolicy: "agent-only"` 的房间由 0 号 Agent 席开局
（`runtime.ts` 已处理，不需要真人房主）。

1000 轮 × 1 秒 ≈ 17 分钟，演示时用 200 轮版本更合适，
但保留 1000 轮作为「极限模式」。

### 完成标准

- 200 轮能跑完且前端不卡（注意：不要每轮都重渲染整张图）
- 收敛曲线能看出趋势
- `pnpm check` 0 错误

---

## 任务卡 · B4 · 镜心斗（♠ 玄渊）

**难度** 中 · **预估** 一天半 · **依赖** 无

### 为什么做这个

你提到的「AI 驱动的同时决策对战」就是这个。它与赛马不重复：
赛马看多方竞速，镜心斗看**双方深度**。

### 规则

三招相克，但每招有「势」1–3 级：

```
攻 破 守，被 闪 化
守 破 闪，被 攻 破
闪 破 攻，被 守 破

同类相遇则高势者胜。
势由前轮结果累积：赢 +1（上限 3），输则归 0。
```

### 为什么适合 AI

它是**有记忆的重复博弈**。势的累积让纳什均衡随局势移动——
AI 要推理「对手是否认为我要出攻」。观众看招式碰撞 + 势的增长条。

### Jev 接入

一次调用三问（`api/world/jev.ts` 已封装好 `askJev`）：

```
move   choice  从三招中择一
level  choice  用几级势
read   score   对手下一招的可预测性 0–2
```

约 300ms，符合回合节奏。**注意顺序**：本地枚举合法出招 → Jev 择一，
不要让 LLM 生成动作（延迟数秒，累积起来观众等不了）。

无 `TYPESAFE_API_KEY` 时要能降级到本地启发式，参照
`api/world/jevJudge.ts` 的 `fallback()` 写法。

### 完成标准

- 2v2 或 1v1 能跑完
- 势的累积与归零逻辑有单测
- Jev 不可用时能降级，且降级路径有测试
- `pnpm check` 0 错误

---

## 任务卡 · B5 · 超能力赛马

**难度** 高 · **预估** 两天 · **依赖** 无（契约已就绪）

### 契约与数据已经写好了

- `contracts/beastRace.ts` — 六兽、八种格子、位移计算、状态结构
- `contracts/beastRace.data.ts` — 48 张技能卡、赛道生成器
- `contracts/beastRace.test.ts` — 37 项测试，**已全绿**

你要做的是**模板执行器与前端演出**，规则设计不用重新想。

### 两条铁律已被测试守住

改数值时如果破了，测试会立刻失败：

1. **克制成环**：不存在「速度最快 + 免疫最多 + 无弱点」的兽
2. **扰行不强于疾行**：否则赛道退化为互相拖后腿的僵局

### 要做的

1. `api/games/sdk/beastRace.ts` — `TemplateModule` 实现
   - `initMatchState` 生成赛道（用 `generateTrack(seed)`）与六匹马初始态
   - `resolveRound` 按「卡牌结算 → 位移 → 格子触发 → 判完赛」顺序
   - 返回 `RaceReveal`，含 `effects[]` 与 `tiles[]`——**这是观众看的因果链**
2. 前端赛道页：横向滚动赛道 + 六匹马 + 手牌区
3. 演出：卡牌飞出命中、位移飘字、雷云击落、沼泽拖拽

### 演出的关键：分步

不要一次性显示结果。节奏（每步 400–600ms）：

```
1. 全员出牌背面朝上落定     ← 悬念峰值，现在的牌桌缺这个停顿
2. 逐个翻开（按提交顺序）
3. 结算效果（飘字 + 位移）   ← 因果显现
4. 新局势定格
```

可复用 `src/components/game/RewardFly.tsx` 做飘字，
GSAP 时间线做编排（`src/lib/gsap.ts` 已封装）。

### 完成标准

- 六人局能跑完（有马冲线即提前终局）
- 观众能看懂「谁因为什么前进/后退了」
- 动效能被 `prefers-reduced-motion` 覆盖
- `pnpm check` 0 错误，37 项契约测试仍全绿

---

## 任务卡 · B6 · 残章拍卖 + 夜话（氛围类）

**难度** 低 · **预估** 各半天 · **依赖** 无

### 残章拍卖（♦ 金壤）

全支付拍卖：所有人出价竞拍一张残章，**出价一律扣除，只有最高者得卡**。

价值：一轮定胜负，30 秒一局，**适合作为开场暖场**。
且它让玩家真切感到碎片是稀缺的——这比任何数值提示都有效。

实现极简：`numberGuess` 模板改判定规则即可（最高者胜而非最接近均值）。

### 夜话（♥ 丹丘）

**不是博弈，是叙事。** 玩家轮流讲述自己这十日的经历（从选项中选），
其他人（含 AI）投票选出「最可信的谎」。

价值有二：给紧张的博弈之间一个喘息；**它产出残章**——
玩家讲的故事片段会成为世界观的一部分。

**这让「世界观构筑平台」的定位真正落地**，不只是个口号。

实现：选项 + 投票，机制上与 `pollDuel` 同构。
难的是写选项文案——每个选项要既可能是真也可能是假。

### 完成标准

- 拍卖能跑完且碎片扣除正确（注意：全支付，输家也扣）
- 夜话至少 20 组选项文案
- `pnpm check` 0 错误

---

## 任务卡 · C1 · 观战页 + 裁判席 UI

**难度** 中 · **预估** 一天 · **依赖** 事件流落库（A 号做）

### 为什么这张卡最重要

**裁判机制是本作最大的原创性，但它现在只存在于测试里——评委看不见。**

后端已完全就绪：
- `world.matchEvents` 能按视角投影取事件流
- `world.appeal` 能发起质询并返回裁判团投票
- `world.recentRulings` 能取最新判例

缺的只是前端。

### 悬疑感的实现：观众知道而玩家不知道

这是核心手法。观战页读 `SPECTATOR` 全知投影，所以能看到密态事件
（狼人的夜间刀法、玩家的真实身份）。

视觉上要分层：
- 公开信息 → 正常显示
- 密态信息 → **暗红描边 + 低透明度**，表示「只有你（观众）看得见」

观众会替玩家着急——这种张力比单纯的推理更有戏剧性。

### 裁判席 UI

三席（或五席、七席）裁判各一枚徽记，投票时逐个亮起：

```
J0 算律（algorithm）  无立场，只读条款
J1 明镜（ai）         Jev 裁断，有支持度数值
J2 众议（agent）      他人影从充任，他们也有立场
```

每席显示：名号、类型、投票（采纳/驳回）、理由一句。
`world.appeal` 的返回里 `jevMeta` 有每席的耗时与支持度，
**把 Jev 的 300–700ms 延迟显示出来**——这是「快判断」的证据。

### 要做的

1. `src/pages/Spectate.tsx` — 观战页（读事件流演出）
2. `src/components/world/JudgePanel.tsx` — 裁判席
3. `src/components/world/AppealModal.tsx` — 质询发起面板
   （条款列表从 `world.rulebook` 取，只显示 `appealable: true` 的）
4. 大厅「观战」按钮接真房间（现在是跳到新对局，是假的）

### 完成标准

- 能回放一局完整对局
- 裁判投票有逐席亮起的演出
- 质询面板能发起真实质询并显示裁决
- `pnpm check` 0 错误

---

## 任务卡 · C2 · 新手引导实现

**难度** 低 · **预估** 半天 · **依赖** 无

### 数据已写好

`contracts/onboarding.data.ts` 九步，含文案、路由、动作类型、奖励提示。
末步落在「规则可被质询」——**这是全作唯一「玩家能改变世界」的机制**。

### 要做的

1. `src/components/onboarding/OnboardingOverlay.tsx` — 浮层
2. 进度存 localStorage（`ONBOARDING_VERSION` 变更时重新引导）
3. 按 `route` 决定当前页是否显示（用 `stepsForRoute()`）

### 实现要点

- `anchor` 为 null 或找不到元素时**降级为居中浮层**，不要报错也不要飘在角落
- `action: "wait"` 的步骤由页面主动调 `advance()`（如结算完成后）
- 引导可跳过，但跳过按钮别做太显眼

### 完成标准

- 九步能走完
- 刷新后进度保留
- 跳过后不再弹出
- `pnpm check` 0 错误

---

## 任务卡 · C3 · 视觉补强 + 待补素材

**难度** 低 · **预估** 一天 · **依赖** 无

### 先读硬约束

`DESIGN.md` 第八节有完整七条，最关键的三条：

1. 暗色唯一，不引入亮色模式，不改 body 的星云 + 噪点两层
2. 四花色语义色固定（♠#8B93F8 ♥#EE6A72 ♣#4ECB9C ♦#F2A93B），
   且 `src/index.css` 与 `src/data/echoes.ts` 两处必须同步
3. 所有新色值用 CSS 变量定义在 `:root`，不写死在组件里

### 优先优化的两页

`/world`（102 行）与 `/agent`（127 行）明显比其他页薄
（狼人杀 1146 行、猜数 749 行）。

### 待补素材

| 优先级 | 素材 | 数量 | 用途 |
|---|---|---|---|
| P0 | 裁判席徽记 | 3 | J0 算律 / J1 明镜 / J2 众议 |
| P0 | 判例卡卡面 | 1 | judicial 唯一稀有度，需明显区别于残章卡 |
| P1 | 观战页幕布 | 2 | 观战的舞台感 |
| P1 | 六兽立绘 | 6 | 超能力赛马（鹤狼龟狐鲤猿） |

### 素材纪律

- 命名 `{类别}-{标识}.{png|svg}`，小写连字符，**不用中文文件名**
- 图标几何图形用 SVG，立绘背景用 PNG
- **入库前跑压缩**：`node scripts/compress-assets.mjs`
  （已把 183MB 压到 81MB，别让它涨回去）

### 完成标准

- 两页视觉与其他页一致
- 新素材已压缩
- `pnpm check` 0 错误

---

## 任务卡 · A1 · 事件流落库（你自己做）

**难度** 中 · **预估** 半天 · **依赖** 无
**优先级 P0 —— 这是全项目投入产出比最高的一件事**

### 为什么是你做

它卡住了六个已完成的系统。核实过的现状：
`grep -rln "matchStart"` 只命中落库层、消费者、测试——
**没有任何代码产出 `MatchEvent`**。

因此这些全都悬空：
- 彩蛋判定器 269 行 + 33 项测试，`detectEggs` **零调用方**
- 残章数据 488 行，常规对局不掉卡（`grantCard` 只在质询路径被调）
- 观战 / 回放 / 情报卡 / 隐藏任务，全部无数据源
- 裁判团暂用「房间码 + 开局时刻」凑种子

### 要做的

在 `api/games/sdk/runtime.ts` 现有节点埋点（事件类型都已定义好）：

```
入座完成 → matchStart      发身份 → secretAssign（打 secret 标记）
每轮开始 → roundBegin      玩家交 → action
揭晓     → reveal          淘汰   → eliminate
结算     → matchEnd
```

然后结算时：

```ts
const logId = await insertMatchLog({...});      // 已写好
const eggs = detectEggs(events);                // 已写好
for (const hit of eggs) await grantCard({...}); // 已写好
```

三个函数都已就绪，你只需要把它们串起来。

### 注意

- 给对局生成真实 `seed` 并存进状态（现在 `SdkRoomState` 没有这个字段）
- `secretAssign` 必须打 `secret: true`，否则投影泄漏
- 落库失败不应让对局结算失败（用 `.catch()` 兜住并记日志）

---

## 文件归属表（防冲突）

| 路径 | 归属 | 规则 |
|---|---|---|
| `contracts/` | **共有** | 改动前群里说一声，这是三方共享的地基 |
| `api/world/` | A | |
| `api/games/sdk/runtime.ts` | A | B 号加模板不要改这个文件 |
| `api/games/sdk/<你的游戏>.ts` | B | 一人一文件，零冲突 |
| `api/queries/` | A 主 B 次 | 一人一文件，不共编 |
| `src/pages/` `src/components/` | C | |
| `src/index.css` | **只有 C 能改** | 避免色彩令牌被覆盖 |
| `db/schema.ts` | A | 改表结构必须群里通知，其他人要 `pnpm db:push` |

### `contracts/` 的特殊纪律

它被三方同时引用，是唯一的高冲突区：

1. **只增不改**：加新类型、加可选字段随意；
   改已有字段名或删字段 → 必须群里确认
2. 一人一文件：B1 建 `pirateGold` 相关，B2 建 `davinci.ts`，不交叉
3. 改完立刻 `pnpm check` + 推送，别攒着

---

## Git 流程

不用 GitFlow，太重。主干 + 短分支：

```bash
git checkout main
git pull --rebase origin main          # 先同步，避免分叉
git checkout -b feat/pirate-gold       # 前缀：feat/ fix/ art/

# 开发中小步提交
git add -A && git commit -m "feat: 海盗分金提案与表决"

pnpm check                             # 必须 0 错误才能推
git push -u origin feat/pirate-gold
# GitHub 开 PR → 另一人瞄一眼 → 合并
```

### 三条铁律

1. **`main` 永远可跑**：推 main 前必须 `pnpm check` 通过
2. **`pull --rebase` 而非 `pull`**：保持历史线性，冲突好解
3. **每天至少推一次**：72 小时的比赛，攒代码是最大风险

### AI 生成的代码必须有人过目

这一条别省。我这次让 codex 生成新手引导文案，文案写得好，
但它**虚构了九个 CSS 选择器**（项目里零命中），引导会指向空气。
两分钟的 review 就能发现。

---

## 建议的排期

### 第一天

| 谁 | 做什么 |
|---|---|
| A | 事件流落库（P0）→ 残章组判定 |
| B | 海盗分金 |
| C | 新手引导实现 → 开始观战页骨架 |

**第一天结束时全员合并一次**，确保 main 能跑通一局完整对局。

### 第二天

| 谁 | 做什么 |
|---|---|
| A | 双身份狼人杀 + 隐藏任务 |
| B | 超能力赛马 或 达芬奇密码（二选一） |
| C | 观战页 + 裁判席 UI |

**第二天结束再合并一次** + 端到端演练。

### 第三天

上午补缺口与修 bug，下午**冻结代码**只修致命问题，晚上排练演示。

### 别忘两件小事

- **字体自托管**（30 分钟）：现在走 Google Fonts CDN，
  现场断网则印章字失效，观感差很多
- **录屏备份**（一小时）：现场网络或数据库故障是常见事故

---

## 演示重点（按原创性排序）

1. **分布式裁判 + 规则演化**（最大亮点，留最多时间）
   现场让 Agent 质询一条规则 → 3 席投票 → 判例入库 → 下一局规则已变

   可直接报的实测数字：Jev 裁断一次质询 755ms，
   实质质询支持度 0.75、论据 1.97/2 判采纳；
   空泛不服支持度 0.43、论据 0.00/2 判驳回。**判别力清晰**是机制可信的关键。

   一句话说清价值：*原著的主角在规则内求生，我们的玩家可以修改规则本身。*

2. **任意 Agent 接入**
   现场用 curl 把评委的 Agent 接进一局。`skills/tdg-agent/SKILL.md` 已写好，
   评委在自己的 Claude Code 里一句话就能生成接入代码。

3. **快慢分工的混合架构**
   Jev 做系统一快判断（300–800ms），LLM 留给系统二慢思考。
   讲清为什么裁判不能用 LLM：裁决在对局中途，全场等待。

4. **四种对抗模式 + 席位策略**
   一个 `SeatKind` 统一表达，`SeatPolicy` 三档强制约束（含 agent-only）。

5. **异能只改信息不改胜负**
   **最容易被低估的加分项**：它证明你们想过架构，不是堆功能。
