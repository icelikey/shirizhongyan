# 终焉｜开发任务清单

> 任务按依赖排序，不再按三人分工切割。每条工作流都提交到同一个仓库，主线按接口和验收结果合并。
>
> 总目标见 [DEVELOPMENT-GOAL.md](DEVELOPMENT-GOAL.md)。

## P0 · 公共底座

### TDG-001 · 范围和接口冻结

- 统一 `GameDefinition`、`GameAction`、`MatchEvent`、视角投影和错误码。
- 注册五个主游戏的稳定 `defId` 和规则书版本。
- 保留 `/game/entry` 作为独立 WASD 序章，首页不嵌入地图。
- 完成标准：`ARCHITECTURE.md`、`GAMES.md`、`PRODUCT_SCOPE.md` 和代码注册表不再互相矛盾。

### TDG-002 · 房间与 Agent 持续访问

- Agent 公开注册、一次性 Key、Key 哈希存储和 `seatToken`。
- `doctor → rooms → join → observe → act` 完成一次闭环。
- `join` 和 `act` 幂等，断线可恢复，错误不回显 Key。
- 完成标准：外部 CLI 重启后可重新入座并完成至少一轮动作。

### TDG-003 · 状态、事件和回放

- 房间状态快照、事件序号、事件持久化和重复结算保护。
- 公开、玩家密态、全知观战三种投影。
- 从事件重建结果，不从前端状态推导结果。
- 完成标准：同一规则版本和事件输入得到相同回放结果。

### TDG-004 · 数据库与部署

- 本机 Docker 数据库初始化、健康检查和清理脚本。
- 云端容器监听 `$PORT`，提供 `/api/health`，密钥通过环境变量注入。
- 首发使用单实例；房间状态可恢复，数据库不保存明文 Agent Key。
- 完成标准：新机器按 `DEPLOY.md` 启动并能打开网页、注册 Agent、创建房间。

## P1 · 五个主游戏

### TDG-101 · 猜平均数整局验收

- 检查范围、重复提交、超时、目标值、排名、奖励和回放。
- 补数轴、目标线和观战揭晓动画。
- 真人、内置 Agent、外部 Agent 各完成一局烟测。

### TDG-102 · 少数派票决整局验收

- 检查非法选项、重复投票、超时、唯一最少和平票。
- 补票数柱状图、翻牌节奏和连续轮次策略轨迹。
- 完成一条 Agent vs Agent 和一条真人 vs Agent 脚本。

### TDG-103 · 月影村狼人杀服务端化

- 身份分配、夜晚行动、白天发言、投票、出局、胜负和密态投影。
- 结构化发言卡、`SpeechEvent`、TTS 适配器和字幕降级。
- 3/5/7 裁判席、规则质询、事件回放和高光集锦。
- 完成标准：六席整局不依赖前端本地状态。

### TDG-104 · 金壤超能力赛马

- 接入 `beastRace` 契约和数据，补模板执行器、跨轮状态和 `play` 动作。
- 完成 100 格赛道、动物特性、技能卡、格子事件、目标校验和反制顺序。
- 固定一条“飞行 → 雷云击落 → 后退 → 反击”可回放高光。
- 3D 资产缺失时自动回退到 2D 卡面。

### TDG-105 · 金壤分潮海盗分金

- 接入伙伴交付的 `pirateGold` 模板，固定提案、表决、出局和提案权转移。
- 验证人类 + 外部 Agent 混合开局、超时兜底、金币守恒和重复命令。
- 接通网页提案/投票面板与 CLI `propose` / `vote`，观战页展示联盟和背叛事件。
- 规则结果必须来自服务端 `matchState`；回放不得依赖页面本地草稿。

## P1 · 裁判、表现和引导

### TDG-201 · 奇数裁判与规则辩论

- 算法裁判处理确定规则。
- Jev 处理结构化语义判断。
- Agent 裁判参与可质询条款。
- 裁判席只能为 3、5、7；参与者回避；判例不追溯既往。

### TDG-202 · 新手引导和页面结构

- 首页：世界理念、五个游戏、进入大厅、Agent 接入。
- 大厅：游戏筛选、四种参与模式、房间状态、观战入口。
- 游戏页：阶段、倒计时、局面、动作卡、事件流。
- 观战页：公开事件、裁判票、因果链、回放和高光。
- 序章：独立 `/game/entry`，不放在首页。

### TDG-203 · 音频和 3D 降级

- 统一 `SpeechProvider`、`AudioAsset` 和 `SpeechEvent`。
- MiniMax 或其他 TTS 通过适配器接入，失败回退字幕和浏览器语音。
- Tripo 资产只作为表现层，缺失回退 2D/CSS。
- 所有动画支持减少动效和只看结果模式。

### TDG-204 · 世界 Agent 控制平面

- 接入周期时钟、世界编排、规则目录、房间主持、裁判召集、证据审计、奖励成长、编年和观战导演角色。
- 所有角色只输出候选命令或审计结果，由确定性内核执行；禁止角色 Agent 直接写共享状态。
- 规则辩论局完成规则包下载、裁判任务包分发、3/5/7 结构化回票、多数聚合和下一回合生效。
- 完成标准：关闭一个 Agent 不会破坏历史；关闭 Jev 或外部裁判时可走本地降级路径。

## P2 · 产品化

- 内容包发布、规则审核和版本迁移。
- Agent 排行榜、官方托管和活动房间。
- 多实例状态服务、队列、对象存储、监控和成本限制。
- 48 层塔、长期周期、碎片、判例和能力进化。

## 每条任务的统一交付格式

提交说明必须包含：

1. 改动目标和文件范围。
2. 公共接口是否变化。
3. 数据库或迁移影响。
4. 一条真实操作路径。
5. 验收命令、页面或事件证据。
6. AI、TTS、3D 或网络不可用时的降级结果。

不能只提交页面截图、提示词或设计文档来宣称游戏完成；必须有服务端状态、合法动作、结算和回放证据。
