# 《十日牌局 · Ten Days Gambit》架构设计

> 本文档从现有代码反向抽取 + 补全构想。凡标注 ✅ 的是已实现并通过类型检查的部分，
> 标注 🚧 的是有契约无实现，标注 📋 的是待设计。

---

## 一、一句话定位

浏览器内即开即玩的「分布式智斗世界」：真人与 AI 智能体同桌博弈，
以《十日终焉》式世界观串联——赢游戏、集碎片、渡十日、登天梯。

---

## 二、分层架构（已落地的四层）

```
┌──────────────────────────────────────────────────────────┐
│  L4 表现层  src/pages (19 页) + src/components            │
│             React 19 + Tailwind + shadcn/ui + GSAP        │
└────────────────────────┬─────────────────────────────────┘
                         │ tRPC（类型安全，无手写 API 契约）
┌────────────────────────┴─────────────────────────────────┐
│  L3 服务层  api/  (2271 行)                               │
│   router: auth / profile / room / game / agent            │
│   SDK 运行时: api/games/sdk/runtime.ts (625 行)           │
│   世界规则: api/world/eggs.ts 彩蛋判定（纯函数）           │
└────────────────────────┬─────────────────────────────────┘
                         │ contracts/ 前后端共享类型 + zod 边界
┌────────────────────────┴─────────────────────────────────┐
│  L2 契约层  contracts/ (1550 行) ★架构的核心★             │
│   gameSdk / matchLog / cards / ability / relics / spire   │
└────────────────────────┬─────────────────────────────────┘
                         │ Drizzle ORM
┌────────────────────────┴─────────────────────────────────┐
│  L1 数据层  db/schema.ts  7 张表 (MySQL)                  │
└──────────────────────────────────────────────────────────┘
```

### 为什么契约层是核心

`contracts/` 被前端、后端、测试三方共同引用，且定义了**三条不可违背的铁律**。
这三条铁律是整个架构能横向扩展小游戏的原因：

| 铁律 | 出处 | 工程意义 |
|---|---|---|
| 卡牌不参与胜负计算 | `cards.ts` | 新增小游戏无需重新平衡卡池 |
| 异能只改写「信息」与「时间」，不改胜负 | `ability.ts` | 48 种异能 × N 游戏，只需 5 个接入点 |
| 密态事件只出现在全知投影 | `matchLog.ts` | 新增密态事件打个 `secret` 标记即自动防泄漏 |

**这是本项目最值得保护的设计资产。** 任何新功能提案，先问是否破坏这三条。

---

## 三、四种对抗模式如何落地 ✅

你构想的四种模式已经由 `SeatKind` 这一个类型统一表达：

```ts
type SeatKind = "human" | "external-agent" | "echo-bot"
```

| 你的构想 | 实现方式 | 状态 |
|---|---|---|
| (a) 用户 vs 用户 | 多个 `human` 席 | ✅ |
| (b) 用户 AI vs 用户 AI | 多个 `external-agent` 席（API Key 接入） | ✅ |
| (c) 用户+AI vs 用户+AI | `human` + `external-agent` 混座 | ✅ |
| (d) 用户 vs AI 混合 | `human` + `echo-bot`（内置八影从） | ✅ |

**外部 Agent 接入链路**（`api/agentRouter.ts`，已实现）：

```
register → 得 tdg_ 前缀 API Key（库内只存 sha256，明文仅返回一次）
   ↓
gatewayRooms  查可见房间
   ↓
gatewayJoin   入座，得与 Key 绑定的 seatToken
   ↓
gatewayObserve 座位视角观测（密态已过滤）
   ↓
gatewayAct    提交动作
```

这套设计的好处：**任何语言、任何框架的 Agent 都能接**，只要会发 HTTP。
黑客松演示时，可以让评委现场用 curl 接一个 Agent 进来打。

---

## 四、小游戏矩阵

### 已实现 ✅

| 游戏 | 花色 | 位置 | 规模 | 联机 |
|---|---|---|---|---|
| 月影狼人杀（6人） | ♠ 玄渊 | `src/engine/werewolfEngine.ts` | 1041 行 | 纯前端 |
| 猜平均数 2/3 | ♣ 青野 | `src/engine/guessEngine.ts` | 445 行 | ✅ 服务端 |
| 红眼病投票博弈 | ♣ 青野 | `api/games/sdk/pollDuel.ts` | 111 行 | ✅ 服务端 |
| 丹丘牌楼（单人爬楼） | ♥ 丹丘 | `src/engine/poker/` | 823 行 | 单人 |
| 碎境爬塔（类杀戮尖塔） | — | `src/engine/spire/` | 1142 行 | 单人 |

### 黑客松建议收敛到这四个

评委看的是**AI 对抗的观赏性**，不是游戏数量。建议主推：

1. **猜平均数** — 最能体现 level-k 思维层级，AI 的"算"肉眼可见 ✅ 已联机
2. **月影狼人杀** — 最有戏剧性，AI 的悍跳/倒钩有表演张力 ⚠️ 需补联机
3. **红眼病投票** — 规则最简，30 秒讲清，适合开场 ✅ 已联机
4. **海盗分金** 📋 — 建议新增，理由见下

**为什么补海盗分金（♦ 金壤）**：它是唯一能体现「AI 谈判与联盟背叛」的博弈，
且规则可由 `numberGuess` 模板小改复用（提案数额 + 投票通过），开发成本低于狼人杀联机。

### 其他适合 AI 对抗的候选 📋

按「观赏性 ÷ 开发成本」排序：

- **阿瓦隆**（♠）— 隐藏身份 + 任务投票，比狼人杀更适合 AI（无需自由发言，全是结构化投票）
- **拍卖博弈 / 全支付拍卖**（♦）— 一轮定胜负，最适合做开场暖场
- **囚徒困境循环赛**（♥）— 经典且观众秒懂，可做 AI 人格的"道德测试场"
- **海龟汤**（♥）— 你提到的裁判机制最佳载体，但需要 AI 裁判，成本较高

---

## 五、分布式裁判机制（你的构想 7）

### 三类裁判的定位

| 类型 | 适用场景 | 实现方式 | 状态 |
|---|---|---|---|
| **算法裁判 J0** | 规则确定的游戏（猜数、投票） | 纯函数，即现有 `resolveRound` | ✅ |
| **AI 裁判 J1** | 需要语义判断（海龟汤、发言合规） | LLM 调用，3/5/7 席投票 | 🚧 |
| **Agent 裁判 J2** | 规则质询、争议仲裁 | 玩家 Agent 临时充当 | 🚧 |

### 已有的契约骨架

事件流里已定义质询与裁决（`contracts/matchLog.ts`）：

```ts
AppealEvent  { t: "appeal",  seat, clauseId, assertion }   // Agent 引条款主张改判
RulingEvent  { t: "ruling",  clauseId, upheld, judgeVotes: boolean[] }
```

数据库已有 `rulings` 表沉淀判例，`cards.ts` 已有 `RulingCard`（稀有度 `judicial`，唯一），
`eggs.ts` 已有「驳律者」彩蛋（质询成功奖 120 分，全场最高）。

### 待补的关键设计 📋

**缺失的是 RuleBook 本体**。目前 `rulebookId` 和 `clauseId` 都只是裸字符串，
没有条款正文，因此无从质询。建议这样补：

```ts
// contracts/rulebook.ts（待建）
interface Clause {
  id: string            // 'c-guess-tie'
  text: string          // 条款正文，AI 质询时引用的对象
  /** 该条款是否开放质询（核心规则不可质询，边缘规则可） */
  appealable: boolean
}
interface RuleBook {
  id: string            // 'rb-guess'
  version: number       // 判例会让规则书演化，必须有版本
  clauses: Clause[]
}
```

**裁判团法定人数**必须是奇数（3/5/7），且应满足两条约束：

1. **利益回避**：裁判不能是本局参与者（否则可自裁自胜）
2. **判例不溯及既往**：判例不改判已结算的胜负，只影响本局后续与未来对局
   （这条已写在 `schema.ts` 的表注释里——原因是回放必须可复现）

**这是整个项目最有原创性的机制**，也是黑客松的最大亮点：
*世界的规则由 AI 的博弈真实地演化*。建议留足演示时间。

---

## 六、异能进化系统（你的构想 6）

### 已实现 ✅

五个钩子挂在对局管道上，异能不写进任何具体游戏：

```
H1 onProject      投影修饰：多看 / 看歪
H2 onBeforeDecide 思考修饰：加时 / 加算力（= token 预算）
H3 onSubmit       动作修饰：改手 / 虚晃
H4 onReveal       揭晓修饰：窥见推理链
H5 onSettle       结算修饰：只动碎片，不动胜负
```

八影从各绑定一个开局异能（`contracts/abilities.data.ts`），契约选人即选异能。
**信息结构本身就是博弈**：对手知道你带了「覆言」，但不知道你何时用了。

位阶决定充能次数：黄 1 / 玄 2 / 地 3（+副异能）/ 天 3（+常驻被动）。

### 待补 📋

- 异能数据目前只有 8 条，注释设想 48 种 → 需扩充（可交给 GPT 批量生成，见分工章节）
- **进化路径未设计**：你的构想是"异能可持续进化"，但目前无升级机制。
  建议方案：异能使用次数达阈值 → 触发"词条觉醒"，在同一钩子内强化效果
  （仍不可改写胜负，否则破铁律）

---

## 七、世界观与经济

### 四大陆（对应扑克四花色）

| 花色 | 大陆 | 语义 | 色值 | 主场游戏 |
|---|---|---|---|---|
| ♠ | 玄渊 | 博弈与欺骗 | `#8B93F8` | 狼人杀、阿瓦隆 |
| ♥ | 丹丘 | 心理与谈判 | `#EE6A72` | 牌楼、海龟汤 |
| ♣ | 青野 | 计算与概率 | `#4ECB9C` | 猜平均数、红眼病 |
| ♦ | 金壤 | 资源与竞逐 | `#F2A93B` | 海盗分金、拍卖 |

### 晋级体系

十二生肖段位（鼠→猪）→ 集满 12 生肖印记 + 本花色碎片套组 → 位阶晋升
**天地玄黄**（黄阶→玄阶→地阶→天阶），天阶之上传说有「洪荒」。

### 双货币设计（已实现）

- **碎片**：可消耗（门票、建房、记忆槽），按四花色分别计数
- **积分**：不可消耗，用于天梯排名。`ScoreDelta = base + egg + appeal`

**质询成功给的是积分而非碎片**，这个设计很妙：让"钻研规则"成为一条
独立于"赢对局"的上升路径。

---

## 八、技术栈与运行

```
前端  React 19 + Vite 7 + TS 5.9 + Tailwind 3.4 + shadcn/ui + GSAP + framer-motion
后端  Hono + tRPC 11 + Drizzle ORM + MySQL
鉴权  Kimi OAuth（jose JWT）+ Agent API Key（sha256）
测试  Vitest（eggs.test.ts 33 项全绿）
实时  tRPC 轮询 1.5s（非 WebSocket，黑客松够用）
```

### 本地启动

```bash
cd app
pnpm install
cp .env.example .env     # 必须：填 DATABASE_URL 否则 3 个 DB 测试会失败
pnpm db:push             # 建表
pnpm dev
```

### 验证

```bash
pnpm check                      # tsc -b，当前 0 错误 ✅
pnpm test                       # 需 DATABASE_URL
npx vitest run api/world/eggs.test.ts   # 纯逻辑，无需 DB，33 项全绿
```

---

## 九、裁判机制的实现现状 ✅

原先只有数据库表和契约，现已补齐运行逻辑。

### 已落地

| 模块 | 文件 | 职责 |
|---|---|---|
| 条款与裁决契约 | `contracts/rulebook.ts` | 四条铁律的判定入口 |
| 四本规则书 | `contracts/rulebooks.data.ts` | 猜数/红眼病/狼人杀/海盗分金 |
| 裁判团运行 | `api/world/judges.ts` | 组团、计票、完整裁决 |
| Jev 客户端 | `api/world/jev.ts` | 系统一决策模型，Key 仅走环境变量 |
| AI 裁判 J1/J2 | `api/world/jevJudge.ts` | Jev 裁断 + 降级启发式 |
| 世界层路由 | `api/worldRouter.ts` | 规则书 / 质询 / 判例 / 观战 |
| 三张表查询层 | `api/queries/{matchLogs,rulings,playerCards}.ts` | 此前全项目仅 schema.ts 引用 |

### 四条铁律（86 项测试覆盖）

1. **法定人数恒奇数**（3/5/7）→ 不存在平票，故无需平局规则
2. **核心条款不可质询**（victory/scoring）→ 否则质询退化为攻击裁判
3. **裁判不得为本局参与者** → 否则可自裁自胜
4. **判例不溯及既往** → 否则 `match_logs` 回放不可复现

### Jev 决策模型

TypeSafe AI 的「系统一」模型：不生成文本，只输出带概率的结构化判定。
三种原语对应三种裁判需求：

```
noul   是/否 + 概率  → 质询是否成立、发言是否矛盾
choice 从候选中择一  → 发言风格归类、裁断采纳/驳回/部分成立
score  数值评分      → 主张的论据强度
```

**为什么不用通用大模型做裁判**（三个工程理由）：

1. 延迟：裁决发生在对局中途，全场等待。Jev 311–755ms 实测，LLM 数秒
2. 格式确定性：输出锁定在预定义 schema 内，无需解析与重试
3. 成本：单次约 0.0004 美元，可以在每次发言后都跑

实测判别力（`npx tsx scripts/jev-smoke.ts`）：

| 场景 | 延迟 | 支持度 | 论据强度 | 裁断 |
|---|---|---|---|---|
| 实质质询 | 755 ms | 0.75 | 1.97/2 | 采纳 |
| 空泛不服 | 691 ms | 0.43 | 0.00/2 | 驳回 |
| 发言分析 | 311 ms | 矛盾 0.57 | 识别悍跳 0.88 | — |

判别力清晰是质询机制可信的前提：若任何胡说都能立判例，判例就一文不值。

**降级路径已测试覆盖**：无 `TYPESAFE_API_KEY` 时 J1/J2 自动回落本地启发式，
裁判团仍凑满法定人数，机制完整可演示。黑客松现场网络不可靠，这条必须存在。

---

## 十、剩余缺口（按优先级）

| 优先级 | 缺口 | 影响 |
|---|---|---|
| P0 | **事件流生产端** | `SdkRoom` 不产 `MatchEvent`，`match_logs` 仍未被写入 |
| P0 | **跨层原子提交** | `command_receipts`/`world_outbox` 已存在，但房间 actor、完整事件流和收据尚未纳入同一提交边界 |
| P1 | 狼人杀联机化 | 最有观赏性的游戏只能单机跑 |
| P1 | 观战页 / 裁判席 UI | 后端已就绪，前端无入口 |
| P1 | 海盗分金模板 | 规则书已写，缺 `TemplateModule` 实现 |
| P2 | 异能进化路径 | 构想 6 只有一半（有异能，无进化） |
| P2 | 异能扩充 8→48 | 契约支持，数据只有 8 条 |

**P0 的核心仍是**：`SdkRoom` → `MatchEvent` → `match_logs` 这条链，并把它与已有的 `command_receipts`/`world_outbox` 收口到可恢复的提交边界。
落库查询层和最小命令收据层已就绪，缺的是在 `runtime.ts` 里埋点产出完整事件、写入 `match_logs`，再由 outbox 消费者驱动通知、叙事和高光。

注：质询目前用「房间码 + 开局时刻」派生种子。事件流落地后应改用
对局真实 seed，使裁判团与回放严格一致。
